# Local development environment variables
# These are used by Vite as fallbacks when window.__MINT_CONFIG__ is not set.
VITE_HASURA_ENDPOINT=http://localhost:8080/v1/graphql
VITE_AUTH_SERVER=https://iam.mint.isi.edu
VITE_AUTH_CLIENT_ID=mint-ui
VITE_AUTH_REALM=production
VITE_AUTH_PROVIDER=keycloak
