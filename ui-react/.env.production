# Production environment variables
# In production, env-config.js (generated at container startup) takes precedence.
# These values are build-time fallbacks only.
VITE_HASURA_ENDPOINT=https://graphql.mint.isi.edu/v1/graphql
VITE_AUTH_SERVER=https://iam.mint.isi.edu
VITE_AUTH_CLIENT_ID=mint-ui
VITE_AUTH_REALM=production
VITE_AUTH_PROVIDER=keycloak
