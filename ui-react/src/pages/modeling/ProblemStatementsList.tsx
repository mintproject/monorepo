import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

/** List of modeling problem statements. */
export function ProblemStatementsList() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>ProblemStatementsList</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">
          Problem statements list will be implemented here.
        </p>
      </CardContent>
    </Card>
  );
}
