/**
 * Manual regions page — in progress, same as legacy "This page is in progress".
 */
export function RegionsManual() {
  return (
    <div className="content-page">
      <p className="text-sm text-muted-foreground">
        This page is in progress — it will give you the ability to define regions by manually
        outlining an area.
      </p>
    </div>
  );
}
