/**
 * Mutation builder: transforms flattened form data into Hasura nested insert variables.
 *
 * This module handles the core innovation of the React UI:
 * converting a single form submission into the nested GraphQL mutation
 * that creates DatasetSpecification + VariablePresentation + junction rows atomically.
 *
 * Will be implemented in the configuration form task.
 */

export function buildConfigurationMutation() {
  // TODO: Implement form data → Hasura nested insert transformation
  throw new Error('Not yet implemented');
}
