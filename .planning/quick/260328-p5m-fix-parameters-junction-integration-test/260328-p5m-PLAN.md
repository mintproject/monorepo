---
phase: quick-260328-p5m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model-catalog-api/src/__tests__/junction-integration.test.ts
autonomous: true
requirements: [fix-parameters-junction-test]

must_haves:
  truths:
    - "All 5 parameters/hasIntervention junction tests pass (POST 201, GET populated, PUT replace, PUT no-op, GET final)"
    - "All 21 tests in the junction-integration suite pass (16 existing + 5 fixed)"
  artifacts:
    - path: "model-catalog-api/src/__tests__/junction-integration.test.ts"
      provides: "Fixed parameters junction test with label fields in intervention objects"
      contains: "label.*Test Intervention"
  key_links:
    - from: "junction-integration.test.ts (parameters suite)"
      to: "POST /parameters API endpoint"
      via: "apiRequest with body containing hasIntervention objects that include label"
      pattern: "initialJunction.*label"
---

<objective>
Fix the parameters junction integration test that fails with a NOT NULL violation on the `label` column.

Purpose: The 5 parameters/hasIntervention tests all fail because the nested intervention objects in the junction config lack `label` fields. PostgreSQL requires NOT NULL columns to be satisfied in INSERT values even when ON CONFLICT is used, so the nested Hasura insert for interventions fails with "null value in column label violates not-null constraint".

Output: All 21 junction integration tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@model-catalog-api/src/__tests__/junction-integration.test.ts
@model-catalog-api/src/mappers/request.ts (buildJunctionInserts — line 221 uses on_conflict update_columns: ['label'])
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add label fields to parameters junction test config</name>
  <files>model-catalog-api/src/__tests__/junction-integration.test.ts</files>
  <action>
In the parameters/hasIntervention `junctionCrudSuite` call (around line 466-503), add `label` arrays to both junction objects:

1. Change `initialJunction` from `{ id: interventionId1 }` to `{ id: interventionId1, label: ['Test Intervention 1'] }`
2. Change `replacedJunction` from `{ id: interventionId2 }` to `{ id: interventionId2, label: ['Test Intervention 2'] }`

Root cause: `buildJunctionInserts()` in request.ts creates a nested Hasura insert with `on_conflict: { constraint: 'modelcatalog_intervention_pkey', update_columns: ['label'] }`. PostgreSQL requires the INSERT portion to satisfy NOT NULL constraints even when the conflict path triggers an UPDATE. The `modelcatalog_intervention` table has `label TEXT NOT NULL`, so the nested insert data MUST include `label`.

Do NOT change any other test suites (softwares, modelconfigurations, modelconfigurationsetups) — they already work because their junction targets (modelcatalog_software_category) either have label provided or handle it differently.
  </action>
  <verify>
    <automated>cd model-catalog-api && MINT_API_TOKEN="${MINT_API_TOKEN}" NODE_TLS_REJECT_UNAUTHORIZED=0 npx vitest run src/__tests__/junction-integration.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>All 21 junction integration tests pass (0 failures). The parameters/hasIntervention suite POST returns 201 instead of 400.</done>
</task>

</tasks>

<verification>
- Run full junction integration test suite: all 21 tests pass
- Parameters POST returns 201 (not 400)
- No regressions in other test suites (softwares, modelconfigurations, modelconfigurationsetups)
</verification>

<success_criteria>
- 21/21 junction integration tests pass
- Zero NOT NULL constraint violations
</success_criteria>

<output>
After completion, create `.planning/quick/260328-p5m-fix-parameters-junction-integration-test/260328-p5m-SUMMARY.md`
</output>
