---
phase: quick
plan: 260328-q36
subsystem: model-catalog-api/testing
tags: [integration-tests, junction, variablepresentations, softwareversions, modelconfigurationsetups]
dependency_graph:
  requires: []
  provides:
    - softwareversions hasInputVariable junction integration test coverage
    - modelconfigurationsetups calibratedVariable junction integration test coverage
  affects:
    - model-catalog-api/src/__tests__/junction-integration.test.ts
tech_stack:
  added: []
  patterns:
    - junctionCrudSuite helper with setup/teardown for prerequisite resources
key_files:
  created: []
  modified:
    - model-catalog-api/src/__tests__/junction-integration.test.ts
decisions:
  - Used testIdSuffix 'sv' and 'mcs-cv' to avoid ID collisions with existing suites ('mc', 'mcs', 'param')
  - Separate ID variable sets (vpId1/vpId2 vs cvpId1/cvpId2) prevent cross-suite collisions
metrics:
  duration: 5 minutes
  completed: 2026-03-28T21:51:50Z
  tasks_completed: 1
  files_modified: 1
---

# Phase quick Plan 260328-q36: Add Junction Integration Tests for softwareversions and modelconfigurationsetups Summary

**One-liner:** Added junction CRUD integration tests for softwareversions/hasInputVariable and modelconfigurationsetups/calibratedVariable using VariablePresentation prerequisites with setup/teardown lifecycle.

## What Was Done

Extended `junction-integration.test.ts` with two new `junctionCrudSuite` calls that test variablepresentation-targeting junctions:

1. **softwareversions with hasInputVariable** — Creates two VariablePresentation resources as prerequisites, then runs 5 CRUD tests (POST creates junction, GET verifies, PUT replaces, PUT-without-field preserves, GET final verification). Uses testIdSuffix `sv`.

2. **modelconfigurationsetups with calibratedVariable** — Same pattern using separate VariablePresentation IDs (cvpId1/cvpId2), testIdSuffix `mcs-cv`.

Both suites follow the exact same pattern as the existing `parameters with hasIntervention` suite: prerequisite resources are created in `setup` and destroyed in `teardown`.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add two junctionCrudSuite calls for variablepresentation-targeting junctions | a7ccc32 (model-catalog-api), 36566e0 (mint submodule) | model-catalog-api/src/__tests__/junction-integration.test.ts |

## Deviations from Plan

None - plan executed exactly as written.

## Known Stubs

None - both suites wire real HTTP requests to the live API at MINT_API_URL.

## Self-Check: PASSED

- File modified: `/Users/mosorio/repos/mint/model-catalog-api/src/__tests__/junction-integration.test.ts` - FOUND
- Commit a7ccc32 in model-catalog-api - FOUND
- Commit 36566e0 in mint parent repo - FOUND
- TypeScript compilation (project-level, excluding pre-existing node_modules type errors) - PASSED
