---
phase: quick
plan: 260328-oa7
type: execute
wave: 1
depends_on: []
files_modified:
  - model-catalog-api/src/__tests__/junction-integration.test.ts
autonomous: true
requirements: []
---

<objective>
Add integration tests for junction CRUD across modelconfigurations, modelconfigurationsetups, and parameters resource types.

Purpose: The existing junction-integration.test.ts only covers softwares with hasModelCategory. We need to verify that junction insert (POST), replace (PUT with junction), no-op (PUT without junction), and cleanup (DELETE) work correctly for the other resource types that have junction relationships.

Output: Extended test file with 3 new describe blocks covering all requested resource types.
</objective>

<context>
@model-catalog-api/src/__tests__/junction-integration.test.ts (existing test pattern)
@model-catalog-api/src/mappers/resource-registry.ts (junction definitions)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add junction integration tests for modelconfigurations, modelconfigurationsetups, and parameters</name>
  <files>model-catalog-api/src/__tests__/junction-integration.test.ts</files>
  <action>
Extend the existing junction-integration.test.ts file by adding 3 new describe blocks after the existing softwares suite. Each follows the identical 5-test pattern (POST with junction -> GET verify -> PUT replace junction -> PUT without junction no-op -> GET final verify) plus afterAll cleanup.

**Shared infrastructure changes:**
- Refactor the `apiRequest` helper and constants (BASE_URL, TOKEN, TEST_TIMEOUT, TEST_SUFFIX) to be shared across all suites (they already are at module scope — no change needed).
- Add a shared `createdIds` tracker per resource type for cleanup, OR add to the existing `createdIds` array with the endpoint path so cleanup knows which endpoint to DELETE from. Best approach: create a `cleanup` array of `{endpoint, id}` objects and a single afterAll that iterates it.

**Suite 1: modelconfigurations with hasModelCategory**
- Resource endpoint: `/modelconfigurations`
- Test ID: `https://w3id.org/okn/i/mint/integration-mc-${TEST_SUFFIX}`
- POST body: `{ id, label: ['Integration Test ModelConfiguration'], type: ['ModelConfiguration'], hasModelCategory: [{ id: 'https://w3id.org/okn/i/mint/Economy', label: ['Economy'] }] }`
- PUT replace: change hasModelCategory to Agriculture (same category IDs as softwares test)
- PUT no-op: omit hasModelCategory, update description only
- DELETE cleanup: `/modelconfigurations/${encodedId}`

**Suite 2: modelconfigurationsetups with hasModelCategory**
- Resource endpoint: `/modelconfigurationsetups`
- Test ID: `https://w3id.org/okn/i/mint/integration-mcs-${TEST_SUFFIX}`
- POST body: `{ id, label: ['Integration Test Setup'], type: ['ModelConfigurationSetup'], hasModelCategory: [{ id: 'https://w3id.org/okn/i/mint/Economy', label: ['Economy'] }] }`
- PUT replace: change hasModelCategory to Agriculture
- PUT no-op: omit hasModelCategory, update description only
- DELETE cleanup: `/modelconfigurationsetups/${encodedId}`

**Suite 3: parameters with hasIntervention**
- Resource endpoint: `/parameters`
- Test ID: `https://w3id.org/okn/i/mint/integration-param-${TEST_SUFFIX}`
- For hasIntervention, we need an intervention ID that exists. First create an intervention as test setup:
  - Intervention test ID: `https://w3id.org/okn/i/mint/integration-interv-${TEST_SUFFIX}`
  - POST `/interventions` with `{ id: interventionTestId, label: ['Test Intervention'], type: ['Intervention'] }` as the first test step
  - Track this for cleanup too
- POST body: `{ id, label: ['Integration Test Parameter'], type: ['Parameter'], hasIntervention: [{ id: interventionTestId }] }`
- PUT replace: create a second intervention ID `integration-interv2-${TEST_SUFFIX}`, POST it, then PUT parameter with hasIntervention pointing to the new one
- PUT no-op: omit hasIntervention, update description only
- DELETE cleanup: delete parameter, then delete both interventions

**Refactoring approach for DRY code:**
Rather than copy-pasting the 5-test pattern 3 times, create a helper function `junctionCrudSuite(config)` that generates the describe block. Config shape:
```ts
interface JunctionTestConfig {
  suiteName: string;
  endpoint: string;        // e.g. '/modelconfigurations'
  testIdSuffix: string;    // e.g. 'mc'
  typeArray: string[];     // e.g. ['ModelConfiguration']
  junctionField: string;   // e.g. 'hasModelCategory'
  initialJunction: object; // e.g. { id: '...Economy', label: ['Economy'] }
  replacedJunction: object; // e.g. { id: '...Agriculture', label: ['Agriculture'] }
  initialJunctionId: string; // for find() assertion
  replacedJunctionId: string; // for find() assertion
  setup?: () => Promise<void>;   // optional pre-test setup (create interventions)
  teardown?: () => Promise<void>; // optional post-test cleanup (delete interventions)
}
```
This helper calls `describe.skipIf(!TOKEN)(suiteName, () => { ... })` with the 5 standard tests.

Keep the existing softwares suite as-is (don't refactor it to use the helper — avoid breaking what works). Only use the helper for the 3 new suites.

**Cleanup strategy:**
Move to a single afterAll at module level that cleans up ALL tracked resources. Track as array of `{ endpoint: string, id: string }`. The existing softwares afterAll should be folded into this unified cleanup, OR kept separate (simpler — keep existing code untouched, add new cleanup for new suites).
  </action>
  <verify>
    <automated>cd /Users/mosorio/repos/mint/model-catalog-api && npx tsc --noEmit src/__tests__/junction-integration.test.ts</automated>
  </verify>
  <done>
- junction-integration.test.ts contains 4 describe blocks total (1 existing softwares + 3 new)
- modelconfigurations suite tests hasModelCategory junction CRUD (5 tests)
- modelconfigurationsetups suite tests hasModelCategory junction CRUD (5 tests)
- parameters suite tests hasIntervention junction CRUD (5+ tests including intervention setup)
- All tests use describe.skipIf(!TOKEN) guard
- Each suite has proper cleanup in afterAll
- TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
1. `cd model-catalog-api && npx tsc --noEmit` — no type errors
2. `MINT_API_TOKEN=<token> npm test -- junction-integration` — all new tests pass against live API (manual verification with token)
</verification>

<success_criteria>
- 3 new test suites added covering modelconfigurations, modelconfigurationsetups, and parameters
- Each suite follows the POST->GET->PUT replace->PUT no-op->GET final->DELETE pattern
- Tests are skipped gracefully when MINT_API_TOKEN is not set
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260328-oa7-add-more-integration-tests-for-junction-/260328-oa7-SUMMARY.md`
</output>
