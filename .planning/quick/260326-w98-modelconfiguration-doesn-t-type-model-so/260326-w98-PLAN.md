---
phase: quick-260326-w98
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - model-catalog-api/src/service.ts
  - model-catalog-api/src/mappers/__tests__/request.test.ts
autonomous: true
requirements: [SCOPE-TYPE-DEFAULT]

must_haves:
  truths:
    - "POST to /modelconfigurations succeeds without sending or storing a type field"
    - "POST to /models still assigns type = sdm#Model in the database"
    - "POST to /softwares still assigns type = sd#Software in the database"
    - "Only resources backed by modelcatalog_software table get a type column assigned"
  artifacts:
    - path: "model-catalog-api/src/service.ts"
      provides: "Conditional type assignment in create()"
      contains: "modelcatalog_software"
    - path: "model-catalog-api/src/mappers/__tests__/request.test.ts"
      provides: "Tests documenting type assignment scope"
  key_links:
    - from: "model-catalog-api/src/service.ts"
      to: "resourceConfig.hasuraTable"
      via: "conditional check before setting input['type']"
      pattern: "hasuraTable.*modelcatalog_software"
---

<objective>
Scope the default type URI assignment so it only applies to Model/Software resources (those backed by modelcatalog_software table), not to ModelConfiguration or other resource types whose tables lack a `type` column.

Purpose: The previous quick task (260326-vn8) added `input['type'] = resourceConfig.typeUri` unconditionally in the create() method. However, only `modelcatalog_software` has a `type` column. Setting `type` on resources like `modelcatalog_model_configuration` would attempt to insert into a non-existent column.

Output: Conditional type assignment in service.ts, updated tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@model-catalog-api/src/service.ts
@model-catalog-api/src/mappers/resource-registry.ts
@model-catalog-api/src/mappers/__tests__/request.test.ts

<interfaces>
From model-catalog-api/src/mappers/resource-registry.ts:
```typescript
export interface ResourceConfig {
  hasuraTable: string | null;
  typeUri: string;
  typeName: string;
  typeArray: string[];
  idPrefix: string;
  relationships: Record<string, RelationshipConfig>;
}
```

Key fact: Only `modelcatalog_software` has a `type` column in the database schema.
Resources using this table: softwares, models, empiricalmodels, hybridmodels, emulators, theory-guidedmodels, theory_guidedmodels, coupledmodels.
Resources NOT using this table (examples): modelconfigurations (modelcatalog_model_configuration), modelconfigurationsetups (modelcatalog_model_configuration_setup), parameters, persons, etc.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope type assignment to modelcatalog_software table only</name>
  <files>model-catalog-api/src/service.ts, model-catalog-api/src/mappers/__tests__/request.test.ts</files>
  <behavior>
    - Test: modelconfigurations resourceConfig does NOT have hasuraTable === 'modelcatalog_software', confirming type should not be assigned
    - Test: models resourceConfig DOES have hasuraTable === 'modelcatalog_software', confirming type should be assigned
    - Test: Simulate create flow -- when hasuraTable is 'modelcatalog_software', input gets type field; when it is not, input does NOT get type field
  </behavior>
  <action>
In service.ts create() method (around line 194), wrap the type assignment in a condition:

Change:
```typescript
input['type'] = resourceConfig.typeUri
```

To:
```typescript
// Only set type for resources stored in modelcatalog_software, which is the
// only table with a `type` column (used to distinguish Model subtypes).
if (resourceConfig.hasuraTable === 'modelcatalog_software') {
  input['type'] = resourceConfig.typeUri
}
```

In request.test.ts, update the "default type assignment via resourceConfig.typeUri" test suite:
- Add a test confirming modelconfigurations has hasuraTable !== 'modelcatalog_software' (documents why no type is assigned)
- Add a test confirming that the conditional logic means modelconfiguration POSTs do not include a type field in their Hasura input
- Existing tests for models/empiricalmodels/softwareversions remain valid (softwareversions uses modelcatalog_software_version -- check this: if softwareversions table is NOT modelcatalog_software, it also should NOT get type assigned; update test expectations accordingly)

Important: Check softwareversions hasuraTable. If it is 'modelcatalog_software_version' (not 'modelcatalog_software'), then softwareversions should NOT have type assigned either. Adjust the test that checks softwareversions typeUri assignment -- it should now verify that softwareversions does NOT get type assigned (since its table lacks a type column).
  </action>
  <verify>
    <automated>cd model-catalog-api && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <done>
    - service.ts only assigns input['type'] when resourceConfig.hasuraTable === 'modelcatalog_software'
    - Tests document that modelconfigurations (and other non-software-table resources) do not get type assigned
    - Tests document that models/softwares/empiricalmodels etc. still get type assigned
    - All tests pass, TypeScript compiles cleanly
  </done>
</task>

</tasks>

<verification>
- `cd model-catalog-api && npx tsc --noEmit` compiles without errors
- `cd model-catalog-api && npx vitest run` all tests pass
- Grep service.ts for the conditional: `grep 'modelcatalog_software' model-catalog-api/src/service.ts`
</verification>

<success_criteria>
- POST /modelconfigurations no longer attempts to set a `type` field (which does not exist on modelcatalog_model_configuration table)
- POST /models and other software-subtypes still correctly set type = their canonical typeUri
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/quick/260326-w98-modelconfiguration-doesn-t-type-model-so/260326-w98-SUMMARY.md`
</output>
