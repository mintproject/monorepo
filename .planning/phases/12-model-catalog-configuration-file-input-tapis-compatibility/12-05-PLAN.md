---
phase: 12-model-catalog-configuration-file-input-tapis-compatibility
plan: 05
type: execute
wave: 3
depends_on:
  - "12-02-PLAN.md"
  - "12-03-PLAN.md"
files_modified:
  - ui/src/screens/models/configure/resources/model-configuration.ts
  - ui/src/screens/models/configure/resources/model-configuration-setup.ts
autonomous: false
requirements:
  - D-17
  - D-18
  - D-19
  - D-20

must_haves:
  truths:
    - "Edit mode shows a checkbox per input row in the _inputDSInput editor for both model-configuration.ts and model-configuration-setup.ts"
    - "Read mode shows an 'optional' badge next to the input label for optional inputs (is_optional=true)"
    - "getResources() in both files returns hasInput items with is_optional populated"
    - "setResources/setResourcesAsCopy round-trip preserves is_optional from the server response"
    - "PUT call to v2 API includes is_optional on hasInput items"
  artifacts:
    - path: ui/src/screens/models/configure/resources/model-configuration.ts
      provides: "Optional checkbox in edit mode, badge in read mode, is_optional in getResources"
      contains: "is_optional"
    - path: ui/src/screens/models/configure/resources/model-configuration-setup.ts
      provides: "Same editor updates at Setup level"
      contains: "is_optional"
  key_links:
    - from: ui/src/screens/models/configure/resources/model-configuration.ts
      to: v2 REST API PUT /modelconfigurations/:id
      via: "getResources() returns hasInput[].is_optional; v2 client sends it in PUT body"
      pattern: "is_optional"
    - from: ui/src/screens/models/configure/resources/model-configuration-setup.ts
      to: v2 REST API PUT /modelconfigurationsetups/:id
      via: "setResourcesAsCopy round-trip (line ~284) preserves is_optional"
      pattern: "is_optional"
---

<objective>
Add the optional checkbox and badge to the UI configure editor in both model-configuration.ts and model-configuration-setup.ts. In edit mode, each input row shows a checkbox that controls is_optional. In read mode, optional inputs show an "optional" badge. The getResources()/setResources round-trip preserves the flag through v2 REST API PUT calls.

Purpose: Curators need a way to mark individual inputs optional from the UI without using the REST API directly.
Output: Updated LitElement components with checkbox in edit mode, badge in read mode, and is_optional in the PUT payload.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-CONTEXT.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-PATTERNS.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-02-SUMMARY.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-03-SUMMARY.md

<interfaces>
<!-- model-configuration.ts key lines to read before touching -->
<!-- Line ~197: _inputDSInput editor declaration or wired connector -->
<!-- Line ~293: somewhere input rows are rendered -->
<!-- Line ~828: getResources() — where hasInput items are assembled for PUT -->

<!-- model-configuration-setup.ts key lines -->
<!-- Line ~210: _inputDSInput editor or input list render -->
<!-- Line ~284: setResourcesAsCopy round-trip — MUST preserve is_optional -->
<!-- Line ~318: input render path -->
<!-- Line ~833: getResources() -->
<!-- Line ~959: another path to check -->

<!-- The DatasetSpecification-like input item in these files today has shape: -->
<!-- { id, label, description, has_format, has_dimensionality, position, ... } -->
<!-- After this plan, items in the UI state must also carry: is_optional: boolean -->

<!-- Per D-20: exact TS shape for the local item type is Claude's discretion. -->
<!-- Must round-trip through setResources/getResources without breaking setResourcesAsCopy:284. -->
<!-- Simplest: extend existing DatasetSpecification-like type with is_optional: boolean field. -->
<!-- The field name in the local state matches the API field isOptional OR is_optional — -->
<!-- whichever the v2 API response uses (model-catalog-api returns JSON camelCase: isOptional). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add is_optional to model-configuration.ts input editor and getResources</name>
  <files>ui/src/screens/models/configure/resources/model-configuration.ts</files>
  <read_first>
    - ui/src/screens/models/configure/resources/model-configuration.ts (full file — focus on lines 197, 293, 828)
    - ui/src/screens/models/configure/resources/model-configuration-setup.ts lines 284 (setResourcesAsCopy — understand round-trip contract before modifying model-configuration.ts)
    - .planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-CONTEXT.md §D-18, D-19, D-20
    - .wolf/anatomy.md (grep for any existing badge pattern in the ui directory to find badge style reference)
  </read_first>
  <action>
Read the file carefully before editing. Focus on three areas:

**Area A — Local input item type (D-20):**
Find how the input items are typed in local state. Add `isOptional?: boolean` (or `is_optional?: boolean`) to the local type/interface that the `_inputDSInput` editor uses. Use the same casing as the v2 REST API response body (check what key the API returns — likely `isOptional` camelCase based on openapi.yaml). The field is optional (`?`) so existing items that pre-date this flag default to falsy.

**Area B — Edit mode: checkbox per input row (D-17, D-18):**
In the template section that renders input rows in edit mode (line ~293 or wherever `_inputDSInput` rows are rendered), add a checkbox after or before the existing input row controls. Pattern:
```html
<label style="font-size: 0.85em; display: flex; align-items: center; gap: 4px; margin-top: 4px;">
  <input type="checkbox"
    .checked="${input.isOptional ?? false}"
    @change="${(e: Event) => { input.isOptional = (e.target as HTMLInputElement).checked; this.requestUpdate(); }}"
  />
  Optional
</label>
```
Adapt the event handler to match the existing pattern in the file for other input field changes (observe how other checkboxes or booleans are handled if any exist).

**Area C — Read mode: badge (D-18):**
In the template section that renders input rows in read mode, add an "optional" badge next to the input label when `input.isOptional` is true:
```html
${input.isOptional ? html`<span style="font-size: 0.75em; background: #e8e8e8; border-radius: 3px; padding: 1px 5px; margin-left: 6px; color: #555;">optional</span>` : ''}
```
Match the existing badge style used elsewhere in the configure UI (grep the file for existing badge patterns first).

**Area D — getResources() save path (D-19, line ~828):**
In `getResources()`, where `hasInput` items are assembled for the PUT body, include `isOptional` from the item's local state:
```typescript
hasInput: this._inputs.map(input => ({
    ...input,
    isOptional: input.isOptional ?? false,
}))
```
Adjust to match the exact local state field name and the v2 API key name.

After editing, verify the LitElement component still compiles:
```bash
cd ui && yarn build 2>&1 | grep -i error | grep -v "DeprecationWarning"
```
  </action>
  <verify>
    <automated>grep -c "is_optional\|isOptional" /Users/mosorio/repos/mint/ui/src/screens/models/configure/resources/model-configuration.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isOptional\|is_optional" ui/src/screens/models/configure/resources/model-configuration.ts` >= 3 (type field + checkbox + getResources)
    - `grep "optional" ui/src/screens/models/configure/resources/model-configuration.ts | grep -v "//\|import"` returns lines in the template (badge and/or checkbox label)
    - `grep "getResources\|hasInput" ui/src/screens/models/configure/resources/model-configuration.ts | grep -i "optional"` returns at least 1 match (flag in save path)
    - `cd ui && yarn build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>model-configuration.ts: checkbox in edit mode, badge in read mode, is_optional/isOptional in getResources().</done>
</task>

<task type="auto">
  <name>Task 2: Mirror changes in model-configuration-setup.ts + checkpoint verification</name>
  <files>ui/src/screens/models/configure/resources/model-configuration-setup.ts</files>
  <read_first>
    - ui/src/screens/models/configure/resources/model-configuration-setup.ts (full file — focus on lines 210, 284, 318, 833, 959)
    - ui/src/screens/models/configure/resources/model-configuration.ts (just updated in Task 1 — use as reference for consistent implementation pattern)
    - .planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-CONTEXT.md §D-17, D-19, D-20 (Setup-level editing is independent, no inheritance)
  </read_first>
  <action>
Apply the same four changes as Task 1 to `model-configuration-setup.ts`:

**Area A — Local input item type:** Same extension with `isOptional?: boolean`.

**Area B — Edit mode checkbox:** Same checkbox template in the Setup-level input editor.

**Area C — Read mode badge:** Same "optional" badge template.

**Area D — getResources() at line ~833:** Include `isOptional` in hasInput items.

**Critical — setResourcesAsCopy round-trip (line ~284, D-20):**
`setResourcesAsCopy` copies a Configuration's resources into a Setup. The Setup editor uses this when creating a new Setup from an existing Configuration. Verify that after this call, `isOptional` from the Configuration's inputs is preserved in the Setup's local state. If `setResourcesAsCopy` currently does a spread or shallow copy, it will automatically carry `isOptional` through if the source items already have it. Confirm this is the case. If it reconstructs item objects explicitly, add `isOptional: sourceInput.isOptional ?? false` to the copy.

After editing, verify build:
```bash
cd ui && yarn build 2>&1 | grep -i error | grep -v "DeprecationWarning"
```
  </action>
  <verify>
    <automated>grep -c "is_optional\|isOptional" /Users/mosorio/repos/mint/ui/src/screens/models/configure/resources/model-configuration-setup.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "isOptional\|is_optional" ui/src/screens/models/configure/resources/model-configuration-setup.ts` >= 3
    - `grep "setResourcesAsCopy" ui/src/screens/models/configure/resources/model-configuration-setup.ts | grep -v "//"` exists (round-trip preserved)
    - `cd ui && yarn build` exits 0 with no TypeScript errors
  </acceptance_criteria>
  <done>model-configuration-setup.ts mirrors all changes from Task 1; setResourcesAsCopy round-trip preserves isOptional.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of optional checkbox and badge in the configure editor</name>
  <read_first>No files to read — this is a visual check after Tasks 1 and 2 complete.</read_first>
  <what-built>
The UI configure editor for both ModelConfiguration and ModelConfigurationSetup now has:
- An "Optional" checkbox per input row in edit mode
- An "optional" badge next to the label for optional inputs in read mode
- is_optional/isOptional in the PUT payload when saving
  </what-built>
  <how-to-verify>
1. Start the UI dev server:
   ```bash
   cd ui && yarn start
   ```

2. Navigate to the model configuration configure page for any model that has hasInput entries.

3. Enter edit mode. Verify:
   - Each input row has an "Optional" checkbox.
   - Checkbox is unchecked by default (existing inputs are not optional).
   - Checking the box does not break the page.

4. Check a box to mark one input optional. Save (PUT). Reload the page. Verify:
   - The "optional" badge appears next to that input's label in read mode.
   - The checkbox is still checked in edit mode.

5. Navigate to the ModelConfigurationSetup configure editor for the same model. Verify the same checkbox and badge behavior.

6. Open browser DevTools → Network. Observe the PUT request body. Verify `hasInput` items include `"isOptional": true` for checked inputs and `"isOptional": false` for unchecked.
  </how-to-verify>
  <resume-signal>Type "approved" if checkbox and badge work and PUT body includes isOptional, or describe any issue observed.</resume-signal>
  <acceptance_criteria>
    - Checkbox visible per input row in edit mode
    - Badge "optional" visible in read mode for is_optional=true inputs
    - PUT body hasInput[].isOptional is true/false (not missing)
    - No JS console errors during interaction
    - setResourcesAsCopy path (creating Setup from Configuration) preserves the flag
  </acceptance_criteria>
  <done>Curator can mark inputs optional via UI; PUT body includes the flag; badge displays in read mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser→v2 REST API | PUT /modelconfigurations/:id body contains isOptional in hasInput items |
| v2 API auth | Bearer JWT required for PUT (write); anonymous can GET |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-13 | Tampering | UI checkbox state → PUT payload | mitigate | PUT is JWT-protected (user role required); only authenticated curators can set is_optional; Hasura boolean type rejects non-boolean; no XSS vector in checkbox value (boolean, not string) |
| T-12-14 | Information Disclosure | "optional" badge visible to anonymous | accept | Read access to hasInput (including is_optional) already permitted for anonymous via Hasura select_permissions; badge is non-sensitive metadata |
| T-12-15 | Tampering | setResourcesAsCopy copies is_optional to Setup | accept | Curator explicitly triggers setResourcesAsCopy; Setup-level is_optional is independent (D-17); can be overridden per Setup without affecting Configuration |
</threat_model>

<verification>
After all tasks:
1. `grep -c "isOptional\|is_optional" ui/src/screens/models/configure/resources/model-configuration.ts` >= 3
2. `grep -c "isOptional\|is_optional" ui/src/screens/models/configure/resources/model-configuration-setup.ts` >= 3
3. `cd ui && yarn build` exits 0
4. Human checkpoint approved: badge + checkbox visible, PUT body correct
</verification>

<success_criteria>
- Checkbox per input row in edit mode in both configuration and setup editors
- "optional" badge in read mode when is_optional is true
- getResources() includes isOptional in hasInput items for PUT payload
- setResourcesAsCopy at line ~284 preserves isOptional from Configuration to Setup local state
- yarn build exits 0 with no TypeScript errors
- Human checkpoint approved
</success_criteria>

<output>
After completion, create `.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-05-SUMMARY.md`
</output>
