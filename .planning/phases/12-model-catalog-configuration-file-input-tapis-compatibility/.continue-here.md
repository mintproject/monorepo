---
context: phase
phase: 12-model-catalog-configuration-file-input-tapis-compatibility
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-04-28T23:51:30.000Z
---

# Resume — Phase 12 / Plan 12-05

**Primary handoff:** `.planning/HANDOFF.json` (structured, fully updated this session). Read it first.

<current_state>
Plan 12-05 paused after two real bugs fixed and committed on `gsd/phase-12-is-optional` across model-catalog-api + super.
Awaiting human helm upgrade + visual reverify. DB write path confirmed working via direct Hasura query (`is_optional=true`).
</current_state>

<completed_work>

This session:
- `model-catalog-api` `7109442` — `ci.yml` widened to `branches: '**'` + SAFE_BRANCH tag. Mirror of ui `e0547dd`.
- `model-catalog-api` `9bcbecd` — `response.ts` hoists junction columns onto nested entity. GET now returns `hasInput[].isOptional`.
- super `cde5557` — bumped mca submodule to `9bcbecd`.
- `helm-charts/dynamo-values.yaml` (gitignored) → `model_catalog_api.image.tag = 9bcbecdd44f7c4385d3c28ccab8105def9c357cb`.
- Todo captured: `2026-04-28-widen-ensemble-manager-graphql-engine-ci-branches.md` — same workflow trap on those two repos.

Direct Hasura junction query post-helm-upgrade returned `is_optional:true` — confirmed write path always worked once correct image deployed.
</completed_work>

<remaining_work>

1. Human: `helm upgrade dynamo charts/mint -n mint -f helm-charts/dynamo-values.yaml` to roll mca `9bcbecd`
2. Reload UI ModelConfiguration editor for `7c2c8d5f-322b-4c1c-8a85-2c49580eadde` — verify checkbox shows true on toggled row + 'optional' badge renders
3. `curl /v2.0.0/modelconfigurations/{id}` — verify `hasInput[].isOptional` present in response
4. Write `12-05-SUMMARY.md`
5. `roadmap.update-plan-progress 12 12-05 complete`
6. Phase 12 verification gates (code-review, regression, schema-drift, gsd-verifier)
7. `phase.complete 12` → milestone v2.0 sealed
</remaining_work>

<decisions_made>

- Two-bug split: B1 (CI never built feature branches) + B2 (response.ts dropped junction columns). B2 was masquerading as "write doesn't work" because GET returned no isOptional even after successful write.
- Hoisting in `response.ts` uses `relConfig.junctionColumns` map — generic across all junction relationships, not just is_optional.
- ensemble-manager + graphql_engine workflow widening deferred as todo (not blocking phase 12).
</decisions_made>

<blockers>
- Human action: helm upgrade dev cluster + reverify (see HANDOFF.json human_actions_pending).
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state, completed_tasks + remaining_tasks + decisions
2. `model-catalog-api/src/mappers/response.ts:97-122` — junction hoist code
3. `model-catalog-api/src/service.ts:251-358` — junction write path (already correct in 12-02)

## Critical Anti-Patterns (do NOT repeat these)
- ANTI-PATTERN: assuming "PUT 200 OK + body has field" means DB has field → also need direct Hasura query AND GET round-trip check. Symmetric write+read paths matter.
- ANTI-PATTERN: assuming docker image rolls automatically when feature branch lands → check workflow `branches:` glob and helm pin SHA. `'*'` does NOT match slashed branches.

## Infrastructure State
- Dev cluster k8s namespace=mint
- ui image: `mintproject/mint-ui-lit:854fa751…` deployed
- mca image target: `ghcr.io/mintproject/model-catalog-api:9bcbecdd44f7c4385d3c28ccab8105def9c357cb` (built, NOT YET HELM-UPGRADED)
- ensemble-manager image: `mintproject/ensemble-manager:model-catalog-migration` (stale; 12-04 changes undeployed — see todo)
- All submodules on `gsd/phase-12-is-optional`
- Hasura migration `1771200016000` applied dev only; staging/prod pending

<next_action>
Wait for user to: helm upgrade → reload UI for cfg 7c2c8d5f → verify checkbox+badge → curl GET. If GET response contains `hasInput[].isOptional`, proceed to write 12-05-SUMMARY.md and run phase verification gates.
</next_action>
