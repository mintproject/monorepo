---
phase: 12-model-catalog-configuration-file-input-tapis-compatibility
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql
  - graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/down.sql
  - graphql_engine/metadata/tables.yaml
autonomous: false
requirements:
  - D-01
  - D-02
  - D-03
  - D-04
  - D-05
  - D-06
  - D-11
  - D-12

must_haves:
  truths:
    - "modelcatalog_configuration_input has column is_optional BOOLEAN NOT NULL DEFAULT FALSE (D-01, D-03: boolean type not TEXT/enum)"
    - "Column is on the junction table only — no entity-level flag on modelcatalog_dataset_specification (D-02)"
    - "Existing junction rows default to FALSE (no manual UPDATE needed — D-04)"
    - "Hasura anonymous and user roles can SELECT is_optional (D-05)"
    - "Hasura user role can INSERT is_optional (D-05)"
    - "Hasura GraphQL API exposes is_optional on configuration_input type after metadata apply (D-06)"
    - "ETL files (etl/extract.py, etl/transform.py) are NOT modified — DB DEFAULT FALSE is the sole ETL mechanism (D-11, D-12)"
  artifacts:
    - path: graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql
      provides: "ADD COLUMN migration"
      contains: "ADD COLUMN is_optional BOOLEAN NOT NULL DEFAULT FALSE"
    - path: graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/down.sql
      provides: "Rollback migration"
      contains: "DROP COLUMN is_optional"
    - path: graphql_engine/metadata/tables.yaml
      provides: "Hasura table tracking + permissions for is_optional"
      contains: "is_optional"
  key_links:
    - from: graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql
      to: graphql_engine/metadata/tables.yaml
      via: "hasura migrate apply then hasura metadata apply"
      pattern: "is_optional"
---

<objective>
Add `is_optional BOOLEAN NOT NULL DEFAULT FALSE` to `modelcatalog_configuration_input`, write the Hasura metadata diff to expose it under both roles, and apply the migration + metadata to the live Hasura instance.

Schema design decisions honored here (per D-02, D-03): single boolean column on the junction table only; no entity-level flag on modelcatalog_dataset_specification; no TEXT enum. ETL is untouched (D-11, D-12): the DB DEFAULT FALSE handles existing and future ETL-inserted rows without any ETL code change.

Purpose: Foundation for all subsequent phases. Codegen and adapter changes in Wave 2 depend on the column existing in the GraphQL schema.
Output: New migration directory, updated tables.yaml, and Hasura GraphQL API exposing is_optional on the configuration_input type.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-CONTEXT.md
@.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-PATTERNS.md

<interfaces>
<!-- Existing modelcatalog_configuration_input table (graphql_engine/migrations/1771105509000_modelcatalog_schema/up.sql lines 83-87) -->
CREATE TABLE modelcatalog_configuration_input (
    configuration_id TEXT NOT NULL REFERENCES modelcatalog_configuration(id),
    input_id         TEXT NOT NULL REFERENCES modelcatalog_dataset_specification(id),
    PRIMARY KEY (configuration_id, input_id)
);

<!-- Current tables.yaml entry for modelcatalog_configuration_input (lines 3088-3119) -->
<!-- See PATTERNS.md §"graphql_engine/metadata/tables.yaml — modelcatalog_configuration_input section" for full before/after YAML diff -->

<!-- Migration sequence reference -->
<!-- Most recent: graphql_engine/migrations/1771200015000_thread_model_io_fk/ -->
<!-- New migration: 1771200016000_modelcatalog_configuration_input_is_optional/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SQL migration files</name>
  <files>
    graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql
    graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/down.sql
  </files>
  <read_first>
    - graphql_engine/migrations/1771200015000_thread_model_io_fk/up.sql (numbering and format reference)
    - graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/up.sql (NOT NULL DEFAULT pattern — PATTERNS.md §migration analog)
    - .planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-PATTERNS.md §"graphql_engine/migrations/..." (exact SQL to use)
  </read_first>
  <action>
Create directory `graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/`.

Write up.sql:
```sql
BEGIN;
ALTER TABLE modelcatalog_configuration_input
    ADD COLUMN is_optional BOOLEAN NOT NULL DEFAULT FALSE;
COMMIT;
```

Write down.sql:
```sql
BEGIN;
ALTER TABLE modelcatalog_configuration_input
    DROP COLUMN is_optional;
COMMIT;
```

Key notes (per D-01, D-04):
- NOT NULL DEFAULT FALSE: existing rows backfill to FALSE automatically via ALTER TABLE default clause. No separate UPDATE needed.
- PK (configuration_id, input_id) is unchanged.
- Migration number 1771200016000 is the next in sequence after 1771200015000_thread_model_io_fk.
- Column is BOOLEAN (not TEXT, not Postgres ENUM) per D-03.
- Column lives on modelcatalog_configuration_input (junction) only — NOT on modelcatalog_dataset_specification (entity) per D-02.
- ETL files (etl/extract.py, etl/transform.py) are NOT touched — DB DEFAULT handles it per D-11, D-12.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN is_optional BOOLEAN NOT NULL DEFAULT FALSE" /Users/mosorio/repos/mint/graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql && grep -c "DROP COLUMN is_optional" /Users/mosorio/repos/mint/graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/down.sql</automated>
  </verify>
  <acceptance_criteria>
    - `up.sql` exists and contains exactly: `ALTER TABLE modelcatalog_configuration_input`
    - `up.sql` contains: `ADD COLUMN is_optional BOOLEAN NOT NULL DEFAULT FALSE`
    - `down.sql` contains: `DROP COLUMN is_optional`
    - Both files are wrapped in `BEGIN;` / `COMMIT;`
    - etl/extract.py is NOT modified (verify with git diff)
    - etl/transform.py is NOT modified (verify with git diff)
  </acceptance_criteria>
  <done>Both migration files exist with the exact SQL described above. ETL untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update Hasura tables.yaml — add is_optional to configuration_input permissions</name>
  <files>graphql_engine/metadata/tables.yaml</files>
  <read_first>
    - graphql_engine/metadata/tables.yaml (find the modelcatalog_configuration_input section — lines ~3088-3119)
    - .planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-PATTERNS.md §"graphql_engine/metadata/tables.yaml — modelcatalog_configuration_input section" (before/after YAML diff)
  </read_first>
  <action>
Locate the `modelcatalog_configuration_input` table entry in `graphql_engine/metadata/tables.yaml`.

The current `columns: &id007` anchor lists only `configuration_id` and `input_id`. Update it to:
```yaml
      columns: &id007
      - configuration_id
      - input_id
      - is_optional
```

The anonymous `select_permissions` role has its own INLINE column list (does NOT use the `*id007` anchor). Add `is_optional` to that inline list as well:
```yaml
  - role: anonymous
    permission:
      columns:
      - configuration_id
      - input_id
      - is_optional
      filter: {}
```

The `user` select_permissions uses `*id007` anchor — it will automatically pick up `is_optional` via the anchor once the anchor is updated.

Do NOT add `update_permissions` — junction tables have insert+delete only (per D-05 and Phase 2/3 established pattern).
Do NOT add new roles — only extend the existing `user` and `anonymous` roles.

The full modified block should match PATTERNS.md §"Modified entry":
```yaml
- table:
    name: modelcatalog_configuration_input
    schema: public
  object_relationships:
  - name: configuration
    using:
      foreign_key_constraint_on: configuration_id
  - name: input
    using:
      foreign_key_constraint_on: input_id
  insert_permissions:
  - role: user
    permission:
      check: {}
      columns: &id007
      - configuration_id
      - input_id
      - is_optional
  select_permissions:
  - role: anonymous
    permission:
      columns:
      - configuration_id
      - input_id
      - is_optional
      filter: {}
  - role: user
    permission:
      columns: *id007
      filter: {}
  delete_permissions:
  - role: user
    permission:
      filter: {}
```
  </action>
  <verify>
    <automated>grep -v '^#' /Users/mosorio/repos/mint/graphql_engine/metadata/tables.yaml | grep -c "is_optional"</automated>
  </verify>
  <acceptance_criteria>
    - `tables.yaml` contains `is_optional` at least twice: once inside the `&id007` anchor, once in the anonymous role's inline column list
    - No `update_permissions` block exists for `modelcatalog_configuration_input`
    - No new roles added
    - YAML is valid (no syntax errors that would cause `hasura metadata apply` to fail)
  </acceptance_criteria>
  <done>tables.yaml updated with is_optional in both the insert/user-select anchor and anonymous inline column list.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Apply migration and metadata to Hasura [BLOCKING]</name>
  <read_first>
    - graphql_engine/metadata/tables.yaml (verify Task 2 changes are present before running)
    - graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/up.sql (verify Task 1 is present)
  </read_first>
  <what-built>
Migration file and metadata YAML are written. The live Hasura instance must apply both before codegen in Wave 2 can succeed. Without this step, codegen will run against stale GraphQL types and Wave 2 tests will false-positive against a DB missing the column.
  </what-built>
  <how-to-verify>
Run the following in the graphql_engine directory (or inside the cluster using deploy-hasura.sh if applicable):

```bash
cd graphql_engine
hasura migrate apply --database-name default
hasura metadata apply
```

Then verify the column exists:
```bash
hasura console  # or connect to psql and run:
# SELECT column_name, data_type, column_default, is_nullable
# FROM information_schema.columns
# WHERE table_name = 'modelcatalog_configuration_input'
# AND column_name = 'is_optional';
```

Expected output: one row with `column_name=is_optional, data_type=boolean, column_default=false, is_nullable=NO`.

Verify Hasura GraphQL schema includes the field:
```graphql
query {
  modelcatalog_configuration_input(limit: 1) {
    configuration_id
    input_id
    is_optional
  }
}
```
Expected: query succeeds (not "field not found").
  </how-to-verify>
  <resume-signal>Type "applied" when migrate apply + metadata apply both succeed, or describe the error.</resume-signal>
  <acceptance_criteria>
    - `hasura migrate apply` exits 0 with no error for migration 1771200016000
    - `hasura metadata apply` exits 0
    - `information_schema.columns` query returns is_optional with data_type=boolean and is_nullable=NO
    - GraphQL query selecting is_optional on modelcatalog_configuration_input succeeds
  </acceptance_criteria>
  <done>Hasura reflects is_optional on modelcatalog_configuration_input; Wave 2 codegen can proceed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DBA→Postgres | Migration SQL runs with admin credentials; additive-only change |
| Hasura admin→GraphQL schema | Metadata apply changes column visibility for user + anonymous roles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | tables.yaml permissions | mitigate | Only extend existing roles (user, anonymous); no new role grants; no update_permissions for junction table — verified by acceptance criteria grep |
| T-12-02 | Elevation of Privilege | Hasura role escalation | mitigate | anonymous role gets SELECT only (no insert/delete); user role gets insert+delete (unchanged); is_optional read-accessible to anonymous matches existing configuration_id/input_id select pattern |
| T-12-03 | Denial of Service | Migration rollback | accept | down.sql reverses the ADD COLUMN; rollback window is migration apply time; column is additive and does not break existing queries |
</threat_model>

<verification>
After all three tasks complete:
1. `grep -c "is_optional" graphql_engine/metadata/tables.yaml` returns >= 2
2. `ls graphql_engine/migrations/1771200016000_modelcatalog_configuration_input_is_optional/` returns up.sql and down.sql
3. Hasura console or psql confirms column exists as BOOLEAN NOT NULL DEFAULT FALSE
4. GraphQL query `{ modelcatalog_configuration_input(limit:1) { is_optional } }` succeeds
5. `git diff --name-only HEAD etl/` returns no etl/ files (ETL untouched per D-11, D-12)
</verification>

<success_criteria>
- Migration directory 1771200016000_modelcatalog_configuration_input_is_optional/ exists with up.sql and down.sql
- up.sql: ALTER TABLE modelcatalog_configuration_input ADD COLUMN is_optional BOOLEAN NOT NULL DEFAULT FALSE
- tables.yaml: is_optional in insert (user) anchor + anonymous select inline list
- hasura migrate apply + metadata apply both succeed with exit 0
- GraphQL introspection confirms is_optional field on modelcatalog_configuration_input type
- ETL files not modified (D-11, D-12 honored)
</success_criteria>

<output>
After completion, create `.planning/phases/12-model-catalog-configuration-file-input-tapis-compatibility/12-01-SUMMARY.md`
</output>
