---
phase: 04-critical-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/up.sql
  - graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/down.sql
  - graphql_engine/metadata/tables.yaml
  - /Users/mosorio/repos/model-catalog-api/src/hasura/field-maps.ts
  - /Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts
  - mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts
autonomous: true

must_haves:
  truths:
    - "has_accepted_values TEXT[] column exists on modelcatalog_parameter table"
    - "Hasura tracks has_accepted_values column in ALL modelcatalog_parameter permissions (user and anonymous roles)"
    - "field-maps.ts includes has_accepted_values in modelcatalog_parameter field selection"
    - "custom datasetspecifications handler filters by configuration_id (not model_configuration_id)"
    - "CatalogParameter interface types has_accepted_values as string[] | null"
    - "Adapter fallback for has_accepted_values returns [] not empty string"
    - "Integration test asserts query body contains configuration_id"
  artifacts:
    - path: "graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/up.sql"
      provides: "ADD COLUMN has_accepted_values TEXT[]"
      contains: "has_accepted_values"
    - path: "graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/down.sql"
      provides: "DROP COLUMN rollback"
      contains: "has_accepted_values"
    - path: "graphql_engine/metadata/tables.yaml"
      provides: "Hasura column tracking for has_accepted_values in both user and anonymous permissions"
      contains: "has_accepted_values"
  key_links:
    - from: "mint-ensemble-manager GraphQL queries"
      to: "graphql_engine/metadata/tables.yaml"
      via: "Hasura permission columns include has_accepted_values for both user and anonymous roles"
      pattern: "has_accepted_values"
    - from: "model-catalog-api/src/custom-handlers.ts"
      to: "modelcatalog_configuration_input/output tables"
      via: "WHERE clause uses correct column name"
      pattern: "configuration_id"
    - from: "model-catalog-api integration tests"
      to: "model-catalog-api/src/custom-handlers.ts"
      via: "test assertion verifies configuration_id in query body"
      pattern: "configuration_id"
---

<objective>
Fix two critical bugs blocking E2E model run flows: (1) add missing has_accepted_values column to modelcatalog_parameter with Hasura tracking, and (2) fix wrong column name in custom datasetspecifications handler WHERE filter.

Purpose: Ensemble Manager model run fetches fail because GraphQL queries request has_accepted_values which does not exist, and custom/datasetspecifications returns 500 because the WHERE clause uses the wrong column name.
Output: Hasura migration, metadata update, field-map fix, handler fix, adapter type fix.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-critical-bug-fixes/04-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add has_accepted_values column migration and Hasura metadata</name>
  <files>
    graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/up.sql
    graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/down.sql
    graphql_engine/metadata/tables.yaml
    /Users/mosorio/repos/model-catalog-api/src/hasura/field-maps.ts
  </files>
  <action>
1. Create migration directory `graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/`

2. Create `up.sql`:
```sql
BEGIN;
ALTER TABLE modelcatalog_parameter
    ADD COLUMN has_accepted_values TEXT[];
COMMIT;
```

3. Create `down.sql`:
```sql
BEGIN;
ALTER TABLE modelcatalog_parameter
    DROP COLUMN IF EXISTS has_accepted_values;
COMMIT;
```

4. Edit `graphql_engine/metadata/tables.yaml`: In the modelcatalog_parameter entry, find the YAML anchor `&id006` column list. Add `- has_accepted_values` after `- has_fixed_value` (line 3441) and before `- position`. This anchor propagates to user role insert_permissions and select_permissions (via `*id006` alias) automatically.

5. IMPORTANT: Also edit `graphql_engine/metadata/tables.yaml` for the **anonymous role select_permissions** on modelcatalog_parameter (around lines 3445-3458). This permission block has its column list written out **explicitly inline** -- it does NOT use the `*id006` alias. Find the anonymous role's `select_permissions` columns list and add `- has_accepted_values` after `- has_fixed_value` and before `- position` in that explicit list. Without this step, anonymous users will be unable to select the new column via GraphQL.

6. Edit `/Users/mosorio/repos/model-catalog-api/src/hasura/field-maps.ts`: Find the modelcatalog_parameter field selection string. Add `has_accepted_values` after `has_fixed_value` in the field list string. There are two locations in this file where modelcatalog_parameter fields are listed (around lines 223 and 325/380) -- update all of them.
  </action>
  <verify>
- `ls graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/` shows up.sql and down.sql
- `grep "has_accepted_values" graphql_engine/migrations/1771200002000_*/up.sql` returns the ALTER TABLE line
- `grep "has_accepted_values" graphql_engine/metadata/tables.yaml` returns at least 2 matches (one in &id006 anchor, one in anonymous role select_permissions)
- `grep -A 30 "role: anonymous" graphql_engine/metadata/tables.yaml | grep "has_accepted_values"` confirms anonymous role includes the column
- `grep "has_accepted_values" /Users/mosorio/repos/model-catalog-api/src/hasura/field-maps.ts` returns matches
  </verify>
  <done>Migration files exist with correct ADD COLUMN / DROP COLUMN SQL. tables.yaml includes has_accepted_values in both the &id006 anchor block AND the anonymous role explicit select_permissions column list. field-maps.ts includes has_accepted_values in modelcatalog_parameter field selections.</done>
</task>

<task type="auto">
  <name>Task 2: Fix configuration_id column name and adapter type</name>
  <files>
    /Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts
    mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts
  </files>
  <action>
1. Edit `/Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts`:
   - Line 494: Change `model_configuration_id` to `configuration_id` in the WHERE clause for `modelcatalog_configuration_input`
   - Line 497: Change `model_configuration_id` to `configuration_id` in the WHERE clause for `modelcatalog_configuration_output`
   - IMPORTANT: Do NOT change line 51 where `model_configuration_id` appears in `SETUP_FIELDS` -- that is a column being SELECTED from `modelcatalog_model_configuration_setup` and is correct.

2. Edit `mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts`:
   - Line 51: Change `has_accepted_values?: string` to `has_accepted_values?: string[] | null`
   - Line 153: Change `parameter.has_accepted_values || ""` to `parameter.has_accepted_values || []`

3. Update the relevant integration test for the custom datasetspecifications handler to add a regression assertion. Find the test file that covers the datasetspecifications custom handler (likely in `/Users/mosorio/repos/model-catalog-api/src/__tests__/` or similar test directory). Add an assertion that verifies the query body sent to Hasura contains `configuration_id` (not `model_configuration_id`):
   ```typescript
   expect(callArgs.query).toContain('configuration_id')
   ```
   This ensures the column name fix does not regress in future changes.

4. Run TypeScript compilation check on model-catalog-api:
   `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit`

5. Run tests on model-catalog-api:
   `cd /Users/mosorio/repos/model-catalog-api && npx vitest run`
  </action>
  <verify>
- `grep "configuration_id" /Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts | grep -c "model_configuration_id"` returns exactly 1 (line 51 in SETUP_FIELDS only)
- `grep "configuration_id.*_eq" /Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts` shows `configuration_id` (not `model_configuration_id`) in WHERE clauses
- `grep "has_accepted_values" mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts` shows `string[] | null` type
- `grep "toContain.*configuration_id" /Users/mosorio/repos/model-catalog-api/src/__tests__/*.ts` shows the regression test assertion
- `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` exits 0
- `cd /Users/mosorio/repos/model-catalog-api && npx vitest run` passes
  </verify>
  <done>custom-handlers.ts WHERE clauses use configuration_id (not model_configuration_id). model-catalog-graphql-adapter.ts types has_accepted_values as string[] | null with [] fallback. Integration test asserts query body contains configuration_id for regression coverage. TypeScript compiles clean, tests pass.</done>
</task>

</tasks>

<verification>
1. Migration exists: `ls graphql_engine/migrations/1771200002000_modelcatalog_parameter_accepted_values/`
2. Column tracked (all roles): `grep -c "has_accepted_values" graphql_engine/metadata/tables.yaml` returns >= 2
3. Anonymous role covered: `grep -A 30 "role: anonymous" graphql_engine/metadata/tables.yaml | grep "has_accepted_values"` returns match
4. Field map updated: `grep "has_accepted_values" /Users/mosorio/repos/model-catalog-api/src/hasura/field-maps.ts`
5. WHERE fix verified: `grep "model_configuration_id" /Users/mosorio/repos/model-catalog-api/src/custom-handlers.ts` returns only SETUP_FIELDS reference (line 51)
6. Adapter type fix: `grep "string\[\]" mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts` includes has_accepted_values
7. Regression test: integration test asserts `configuration_id` in query body
8. TypeScript clean: `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit`
9. Tests pass: `cd /Users/mosorio/repos/model-catalog-api && npx vitest run`
</verification>

<success_criteria>
- has_accepted_values TEXT[] column migration exists and Hasura metadata tracks it for ALL roles (user and anonymous)
- field-maps.ts includes has_accepted_values in modelcatalog_parameter selections
- custom-handlers.ts WHERE clauses use configuration_id (not model_configuration_id)
- CatalogParameter interface types has_accepted_values as string[] | null
- Adapter fallback returns [] instead of ""
- Integration test asserts configuration_id appears in query body (regression coverage)
- model-catalog-api compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/04-critical-bug-fixes/04-01-SUMMARY.md`
</output>
