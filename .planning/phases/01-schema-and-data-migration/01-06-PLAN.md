---
phase: 01-schema-and-data-migration
plan: 06
type: execute
wave: 2
depends_on: ["01-05"]
files_modified:
  - etl/transform.py
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "transform_all() produces load-ready rows for all 36 tables"
    - "New entity types are deduplicated and have labels derived from URIs when rdfs:label is missing"
    - "Junction table rows only include pairs where both FK targets exist in extracted data"
  artifacts:
    - path: "etl/transform.py"
      provides: "FK inversion, dedup, label derivation, junction table building for all 36 tables"
      exports: ["transform_all"]
  key_links:
    - from: "etl/transform.py"
      to: "etl/extract.py"
      via: "extracted_data dict keys match between extract and transform"
      pattern: "extracted_data\\["
---

<objective>
Extend ETL transformation to process all 10 new entity types and build 14 new junction tables from the extracted data.

Purpose: With extraction updated (plan 01-05), the transformation module needs to deduplicate new entities, derive labels, resolve self-referential FKs, and build junction table rows for all 14 new relationship tables.

Output: Updated transform.py that produces load-ready data for all 36 modelcatalog tables.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-and-data-migration/01-02-SUMMARY.md
@.planning/phases/01-schema-and-data-migration/01-03-SUMMARY.md
@.planning/phases/01-schema-and-data-migration/01-05-SUMMARY.md
@.planning/phases/01-schema-and-data-migration/01-VERIFICATION.md
@etl/transform.py
@etl/extract.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedup, label derivation, and self-referential FK resolution for 10 new entity types</name>
  <files>etl/transform.py</files>
  <action>
Update `transform_all(extracted_data)` to process the 10 new entity types:

1. Deduplicate each new entity list with `deduplicate_by_id()`:
   - persons, model_categories, regions, processes, time_intervals, causal_diagrams, images, variable_presentations, interventions, grids

2. Apply `ensure_labels()` to all new entity lists (derive labels from URIs when rdfs:label is missing).

3. Add self-referential FK handling for hierarchical entities:
   - `modelcatalog_model_category`: Resolve `parent_category_id` from parent links. For each category, if a parent link exists, set `parent_category_id` to the parent category's ID. Otherwise set to None. Only set FK if the parent category exists in the extracted data (same filtering pattern as existing junction tables).
   - `modelcatalog_region`: Resolve `part_of_id` from part_of links. Same pattern.

4. Ensure existing software_version, model_configuration, and model_configuration_setup rows now carry the new column values from extraction (short_description, limitations, author_id, etc.). These come through the extracted_data dict and should already be present if extraction was done correctly. No special transform needed -- just ensure they pass through to the output.

5. Update the return value of `transform_all()` to include all 10 new entity table rows keyed by table name (e.g., 'modelcatalog_person', 'modelcatalog_model_category', etc.) alongside the existing 6 entity tables.

6. Print summary counts for all new entity tables.
  </action>
  <verify>
Run `python -c "import sys; sys.path.insert(0, 'etl'); import transform; print('transform.py imports OK')"` from the repo root. Grep transform.py for `deduplicate_by_id` to confirm all 10 new types are deduped. Grep for `ensure_labels` to confirm labels are derived.
  </verify>
  <done>
transform_all() deduplicates and labels all 10 new entity types. Self-referential FKs resolved for model_category (parent_category_id) and region (part_of_id). New column values on existing entities pass through. Return value includes all 16 entity table rows keyed by table name.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build 14 new junction tables with FK validation</name>
  <files>etl/transform.py</files>
  <action>
Add a new `build_extended_junction_tables()` function (do NOT modify the existing `build_junction_tables()` function for the original 6 junction tables):

1. Build valid ID sets for new entity types:
   ```python
   valid_person_ids = {e['id'] for e in extracted_data['persons']}
   valid_category_ids = {e['id'] for e in extracted_data['model_categories']}
   valid_region_ids = {e['id'] for e in extracted_data['regions']}
   valid_process_ids = {e['id'] for e in extracted_data['processes']}
   valid_time_interval_ids = {e['id'] for e in extracted_data['time_intervals']}
   valid_causal_diagram_ids = {e['id'] for e in extracted_data['causal_diagrams']}
   valid_image_ids = {e['id'] for e in extracted_data['images']}
   valid_variable_ids = {e['id'] for e in extracted_data['variable_presentations']}
   valid_intervention_ids = {e['id'] for e in extracted_data['interventions']}
   valid_grid_ids = {e['id'] for e in extracted_data['grids']}
   ```

2. For each junction table, iterate the link dict and create rows only when both FK targets exist (same pattern as existing `build_junction_tables`):

   **SoftwareVersion level (6):**
   - `modelcatalog_software_version_category`: {software_version_id, category_id}
   - `modelcatalog_software_version_process`: {software_version_id, process_id}
   - `modelcatalog_software_version_grid`: {software_version_id, grid_id}
   - `modelcatalog_software_version_image`: {software_version_id, image_id}
   - `modelcatalog_software_version_input_variable`: {software_version_id, variable_id}
   - `modelcatalog_software_version_output_variable`: {software_version_id, variable_id}

   **Configuration level (3):**
   - `modelcatalog_configuration_causal_diagram`: {configuration_id, causal_diagram_id}
   - `modelcatalog_configuration_time_interval`: {configuration_id, time_interval_id}
   - `modelcatalog_configuration_region`: {configuration_id, region_id}

   **Setup level (3):**
   - `modelcatalog_setup_author`: {setup_id, person_id}
   - `modelcatalog_setup_calibrated_variable`: {setup_id, variable_id}
   - `modelcatalog_setup_calibration_target`: {setup_id, variable_id}

   **Parameter level (1):**
   - `modelcatalog_parameter_intervention`: {parameter_id, intervention_id}

   **CausalDiagram level (1, polymorphic):**
   - `modelcatalog_diagram_part`: {causal_diagram_id, part_id, part_type}. Set part_type='variable' if part_id is in valid_variable_ids, 'process' if in valid_process_ids. Skip if in neither.

3. Call `build_extended_junction_tables()` from `transform_all()` and merge its results into the return dict alongside the existing 6 junction tables.

4. Print summary counts for all 14 new junction tables.

Important: Do NOT modify the existing junction table building logic for the original 6 junction tables. Only ADD new logic. The existing `build_junction_tables()` function must remain unchanged.
  </action>
  <verify>
Run `python -c "import sys; sys.path.insert(0, 'etl'); import transform; print('transform.py imports OK')"` from the repo root. Grep transform.py for `build_extended_junction_tables` to confirm the function exists. Grep for all 14 new junction table names (e.g., `modelcatalog_software_version_category`, `modelcatalog_diagram_part`).
  </verify>
  <done>
transform_all() produces a dict with keys for all 36 modelcatalog tables. 14 new junction tables built with FK validation (only pairs where both targets exist). Existing 12 tables (6 entity + 6 junction) unaffected. Polymorphic diagram_part junction includes part_type discriminator.
  </done>
</task>

</tasks>

<verification>
1. transform.py imports without errors
2. transform_all() produces output keys for all 36 tables (16 entity + 20 junction)
3. New entity types are deduplicated and labeled
4. Self-referential FKs resolved for model_category and region
5. 14 new junction tables built with FK validation
6. Polymorphic diagram_part junction includes part_type discriminator
7. No regressions: existing transformation logic for original 6 entity types and 6 junction tables unchanged
</verification>

<success_criteria>
- transform_all() returns a dict with 36 table name keys, each containing a list of row dicts
- All new entity types deduped and labeled
- Junction table rows only include valid FK pairs
- Python file imports cleanly without syntax errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-and-data-migration/01-06-SUMMARY.md`
</output>
