---
phase: 01-schema-and-data-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/up.sql
  - graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/down.sql
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All ontology entity types from SDM 1.8.0 have corresponding PostgreSQL tables"
    - "Existing tables have columns for all datatype properties defined in the ontology"
    - "Many-to-many object properties are represented by junction tables"
    - "Migration is additive -- does not modify or drop anything from the existing migration"
  artifacts:
    - path: "graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/up.sql"
      provides: "New entity tables, ALTER TABLE columns, junction tables, indexes"
      contains: "CREATE TABLE modelcatalog_person"
    - path: "graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/down.sql"
      provides: "Complete rollback of all additions"
      contains: "DROP TABLE"
  key_links:
    - from: "modelcatalog_model_configuration_setup.author_id"
      to: "modelcatalog_person(id)"
      via: "REFERENCES FK"
      pattern: "REFERENCES modelcatalog_person"
    - from: "modelcatalog_software_version_category junction"
      to: "modelcatalog_model_category(id)"
      via: "FK on category_id"
      pattern: "REFERENCES modelcatalog_model_category"
    - from: "modelcatalog_configuration_region junction"
      to: "modelcatalog_region(id)"
      via: "FK on region_id"
      pattern: "REFERENCES modelcatalog_region"
---

<objective>
Create a new additive PostgreSQL migration that adds missing ontology entity tables, columns on existing tables, and junction tables for many-to-many object properties from the SDM 1.8.0 ontology.

Purpose: The UAT found that the initial schema (01-01) covered the core 4-level hierarchy and I/O but omitted many ontology properties actively used in production TriG data: ModelCategory, Region, Person, Process, Grid, TimeInterval, CausalDiagram, Image, VariablePresentation, Intervention entities, plus data properties like limitations, parameterization, calibrationMethod, etc.

Output: A new Hasura migration directory with up.sql and down.sql that extends the existing schema without modifying it.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-and-data-migration/01-01-SUMMARY.md
@graphql_engine/migrations/1771105509000_modelcatalog_schema/up.sql
@model-catalog-ontology/release/1.8.0/ontology.ttl
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create extended schema migration (up.sql)</name>
  <files>graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/up.sql</files>
  <action>
Create a new migration directory `graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/` and write `up.sql` containing the following, all wrapped in BEGIN/COMMIT:

**Section 1: New entity tables** (9 tables, all with TEXT PRIMARY KEY following existing pattern):

1. `modelcatalog_person` -- sd:Person entities (authors)
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - name TEXT (sd:name property)

2. `modelcatalog_model_category` -- sdm:ModelCategory
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - parent_category_id TEXT REFERENCES modelcatalog_model_category(id) ON DELETE SET NULL (sdm:parentCategory, self-referential)

3. `modelcatalog_region` -- sdm:Region
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - description TEXT
   - part_of_id TEXT REFERENCES modelcatalog_region(id) ON DELETE SET NULL (sdm:partOf, self-referential)

4. `modelcatalog_process` -- sdm:Process
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL

5. `modelcatalog_time_interval` -- sdm:TimeInterval
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - description TEXT
   - interval_value TEXT (sdm:intervalValue -- can be integer or string per ontology)
   - interval_unit TEXT (sdm:intervalUnit -- stored as URI reference to qudt:Unit)

6. `modelcatalog_causal_diagram` -- sdm:CausalDiagram
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL

7. `modelcatalog_image` -- sd:Image
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - description TEXT

8. `modelcatalog_variable_presentation` -- sd:VariablePresentation
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - description TEXT
   - has_long_name TEXT (sd:hasLongName)
   - has_short_name TEXT (sd:hasShortName)
   - has_standard_variable TEXT (sd:hasStandardVariable -- URI reference)
   - uses_unit TEXT (sd:usesUnit -- URI reference)

9. `modelcatalog_intervention` -- sdm:Intervention
   - id TEXT PRIMARY KEY
   - label TEXT NOT NULL
   - description TEXT

**Section 2: Grid table** (sdm:Grid is a subclass of sd:DatasetSpecification but distinct entity type):

10. `modelcatalog_grid` -- sdm:Grid
    - id TEXT PRIMARY KEY
    - label TEXT NOT NULL
    - description TEXT
    - has_dimension TEXT (sdm:hasDimension)
    - has_shape TEXT (sdm:hasShape)
    - has_spatial_resolution TEXT (sdm:hasSpatialResolution)
    - has_coordinate_system TEXT (sdm:hasCoordinateSystem)
    - grid_type TEXT (to distinguish PointBasedGrid, SpatiallyDistributedGrid, etc.)

**Section 3: ALTER TABLE -- add columns to existing tables** (all nullable since data is already loaded):

On `modelcatalog_software_version`:
- ADD COLUMN short_description TEXT (sd:shortDescription)
- ADD COLUMN limitations TEXT (sdm:limitations -- ontology domain is Model, which SoftwareVersion inherits from)
- ADD COLUMN parameterization TEXT (sdm:parameterization)
- ADD COLUMN runtime_estimation TEXT (sdm:runtimeEstimation)
- ADD COLUMN theoretical_basis TEXT (sdm:theoreticalBasis)

On `modelcatalog_model_configuration`:
- ADD COLUMN has_model_result_table TEXT (sdm:hasModelResultTable)

On `modelcatalog_model_configuration_setup`:
- ADD COLUMN author_id TEXT REFERENCES modelcatalog_person(id) ON DELETE SET NULL (sd:author -- note: in TriG data author can be multi-valued, but most setups have 0-1 author. Use FK column for now; the junction table below handles the multi-valued case)
- ADD COLUMN calibration_interval TEXT (sdm:calibrationInterval)
- ADD COLUMN calibration_method TEXT (sdm:calibrationMethod)
- ADD COLUMN parameter_assignment_method TEXT (sdm:parameterAssignmentMethod)
- ADD COLUMN valid_until TEXT (sdm:validUntil)

**Section 4: Junction tables** for many-to-many object properties (14 junction tables):

Software/SoftwareVersion level (note: in the ontology, hasModelCategory/hasProcess/hasGrid/etc. have domain sdm:Model. In the TriG data, these appear on both ModelConfiguration and ModelConfigurationSetup entities which are subclasses of Model. Create junction tables at the appropriate level based on actual data usage):

11. `modelcatalog_software_version_category` (SoftwareVersion <-> ModelCategory via sdm:hasModelCategory)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - category_id TEXT REFERENCES modelcatalog_model_category(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, category_id)

12. `modelcatalog_software_version_process` (SoftwareVersion <-> Process via sdm:hasProcess)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - process_id TEXT REFERENCES modelcatalog_process(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, process_id)

13. `modelcatalog_software_version_grid` (SoftwareVersion <-> Grid via sdm:hasGrid)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - grid_id TEXT REFERENCES modelcatalog_grid(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, grid_id)

14. `modelcatalog_software_version_image` (SoftwareVersion <-> Image via sdm:hasExplanationDiagram)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - image_id TEXT REFERENCES modelcatalog_image(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, image_id)

15. `modelcatalog_software_version_input_variable` (SoftwareVersion <-> VariablePresentation via sdm:hasInputVariable)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - variable_id TEXT REFERENCES modelcatalog_variable_presentation(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, variable_id)

16. `modelcatalog_software_version_output_variable` (SoftwareVersion <-> VariablePresentation via sdm:hasOutputVariable)
    - software_version_id TEXT REFERENCES modelcatalog_software_version(id) ON DELETE CASCADE
    - variable_id TEXT REFERENCES modelcatalog_variable_presentation(id) ON DELETE CASCADE
    - PRIMARY KEY (software_version_id, variable_id)

Configuration level:

17. `modelcatalog_configuration_causal_diagram` (ModelConfiguration <-> CausalDiagram via sdm:hasCausalDiagram)
    - configuration_id TEXT REFERENCES modelcatalog_model_configuration(id) ON DELETE CASCADE
    - causal_diagram_id TEXT REFERENCES modelcatalog_causal_diagram(id) ON DELETE CASCADE
    - PRIMARY KEY (configuration_id, causal_diagram_id)

18. `modelcatalog_configuration_time_interval` (ModelConfiguration <-> TimeInterval via sdm:hasOutputTimeInterval)
    - configuration_id TEXT REFERENCES modelcatalog_model_configuration(id) ON DELETE CASCADE
    - time_interval_id TEXT REFERENCES modelcatalog_time_interval(id) ON DELETE CASCADE
    - PRIMARY KEY (configuration_id, time_interval_id)

19. `modelcatalog_configuration_region` (ModelConfiguration <-> Region via sdm:hasRegion)
    - configuration_id TEXT REFERENCES modelcatalog_model_configuration(id) ON DELETE CASCADE
    - region_id TEXT REFERENCES modelcatalog_region(id) ON DELETE CASCADE
    - PRIMARY KEY (configuration_id, region_id)

Setup level:

20. `modelcatalog_setup_author` (ModelConfigurationSetup <-> Person via sd:author, multi-valued)
    - setup_id TEXT REFERENCES modelcatalog_model_configuration_setup(id) ON DELETE CASCADE
    - person_id TEXT REFERENCES modelcatalog_person(id) ON DELETE CASCADE
    - PRIMARY KEY (setup_id, person_id)

21. `modelcatalog_setup_calibrated_variable` (ModelConfigurationSetup <-> VariablePresentation via sdm:calibratedVariable)
    - setup_id TEXT REFERENCES modelcatalog_model_configuration_setup(id) ON DELETE CASCADE
    - variable_id TEXT REFERENCES modelcatalog_variable_presentation(id) ON DELETE CASCADE
    - PRIMARY KEY (setup_id, variable_id)

22. `modelcatalog_setup_calibration_target` (ModelConfigurationSetup <-> VariablePresentation via sdm:calibrationTargetVariable)
    - setup_id TEXT REFERENCES modelcatalog_model_configuration_setup(id) ON DELETE CASCADE
    - variable_id TEXT REFERENCES modelcatalog_variable_presentation(id) ON DELETE CASCADE
    - PRIMARY KEY (setup_id, variable_id)

Parameter level:

23. `modelcatalog_parameter_intervention` (Parameter <-> Intervention via sdm:relevantForIntervention)
    - parameter_id TEXT REFERENCES modelcatalog_parameter(id) ON DELETE CASCADE
    - intervention_id TEXT REFERENCES modelcatalog_intervention(id) ON DELETE CASCADE
    - PRIMARY KEY (parameter_id, intervention_id)

CausalDiagram parts:

24. `modelcatalog_diagram_part` (CausalDiagram <-> VariablePresentation|Process via sdm:hasDiagramPart -- polymorphic)
    - causal_diagram_id TEXT REFERENCES modelcatalog_causal_diagram(id) ON DELETE CASCADE
    - part_id TEXT NOT NULL (URI of either VariablePresentation or Process)
    - part_type TEXT NOT NULL CHECK (part_type IN ('variable', 'process'))
    - PRIMARY KEY (causal_diagram_id, part_id)

**Section 5: Indexes** on all FK columns in new tables and junction tables:

Create indexes on:
- modelcatalog_model_category(parent_category_id)
- modelcatalog_region(part_of_id)
- modelcatalog_model_configuration_setup(author_id)
- All junction table FK columns (both sides of each junction)
- modelcatalog_diagram_part(part_id) for lookups by part

Follow the existing naming convention: `idx_mc_{abbreviated_table}_{column}`.

**IMPORTANT constraints:**
- This migration MUST NOT modify any existing table structure or drop/recreate anything from migration 1771105509000. It is purely additive (ALTER TABLE ADD COLUMN and new CREATE TABLE).
- All new columns on existing tables MUST be nullable (no NOT NULL) since data is already loaded.
- The author_id FK on modelcatalog_model_configuration_setup is intentionally redundant with the modelcatalog_setup_author junction table. It serves as a convenience column for the common single-author case while the junction table handles multi-valued cases. Both will be populated.
  </action>
  <verify>
Verify the migration file:
- `ls graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/up.sql` -- file exists
- Count CREATE TABLE statements: should be 24 (10 entity + 14 junction)
- Count ALTER TABLE statements: should be 11 (5 on software_version + 1 on model_configuration + 5 on model_configuration_setup)
- Count CREATE INDEX statements: should be 30+ (self-ref FKs + author_id + all junction table FKs)
- Verify no DROP or ALTER COLUMN statements exist (purely additive)
- The file should start with BEGIN and end with COMMIT
  </verify>
  <done>
up.sql exists with 10 new entity tables, 14 new junction tables, 11 ALTER TABLE ADD COLUMN statements, and indexes on all FK columns. No modifications to existing tables beyond adding nullable columns.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create rollback migration (down.sql)</name>
  <files>graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/down.sql</files>
  <action>
Create `down.sql` in the same migration directory that reverses all changes from up.sql in proper dependency order, wrapped in BEGIN/COMMIT:

1. **Drop junction tables first** (they reference entity tables):
   - DROP TABLE IF EXISTS modelcatalog_diagram_part
   - DROP TABLE IF EXISTS modelcatalog_parameter_intervention
   - DROP TABLE IF EXISTS modelcatalog_setup_calibration_target
   - DROP TABLE IF EXISTS modelcatalog_setup_calibrated_variable
   - DROP TABLE IF EXISTS modelcatalog_setup_author
   - DROP TABLE IF EXISTS modelcatalog_configuration_region
   - DROP TABLE IF EXISTS modelcatalog_configuration_time_interval
   - DROP TABLE IF EXISTS modelcatalog_configuration_causal_diagram
   - DROP TABLE IF EXISTS modelcatalog_software_version_output_variable
   - DROP TABLE IF EXISTS modelcatalog_software_version_input_variable
   - DROP TABLE IF EXISTS modelcatalog_software_version_image
   - DROP TABLE IF EXISTS modelcatalog_software_version_grid
   - DROP TABLE IF EXISTS modelcatalog_software_version_process
   - DROP TABLE IF EXISTS modelcatalog_software_version_category

2. **Remove added columns from existing tables** (reverse order of addition):
   - ALTER TABLE modelcatalog_model_configuration_setup DROP COLUMN IF EXISTS valid_until
   - ALTER TABLE modelcatalog_model_configuration_setup DROP COLUMN IF EXISTS parameter_assignment_method
   - ALTER TABLE modelcatalog_model_configuration_setup DROP COLUMN IF EXISTS calibration_method
   - ALTER TABLE modelcatalog_model_configuration_setup DROP COLUMN IF EXISTS calibration_interval
   - ALTER TABLE modelcatalog_model_configuration_setup DROP COLUMN IF EXISTS author_id
   - ALTER TABLE modelcatalog_model_configuration DROP COLUMN IF EXISTS has_model_result_table
   - ALTER TABLE modelcatalog_software_version DROP COLUMN IF EXISTS theoretical_basis
   - ALTER TABLE modelcatalog_software_version DROP COLUMN IF EXISTS runtime_estimation
   - ALTER TABLE modelcatalog_software_version DROP COLUMN IF EXISTS parameterization
   - ALTER TABLE modelcatalog_software_version DROP COLUMN IF EXISTS limitations
   - ALTER TABLE modelcatalog_software_version DROP COLUMN IF EXISTS short_description

3. **Drop new entity tables** (order matters for self-references):
   - DROP TABLE IF EXISTS modelcatalog_grid
   - DROP TABLE IF EXISTS modelcatalog_intervention
   - DROP TABLE IF EXISTS modelcatalog_variable_presentation
   - DROP TABLE IF EXISTS modelcatalog_image
   - DROP TABLE IF EXISTS modelcatalog_causal_diagram
   - DROP TABLE IF EXISTS modelcatalog_time_interval
   - DROP TABLE IF EXISTS modelcatalog_process
   - DROP TABLE IF EXISTS modelcatalog_region
   - DROP TABLE IF EXISTS modelcatalog_model_category
   - DROP TABLE IF EXISTS modelcatalog_person

Note: Indexes are automatically dropped when their tables are dropped. The FK indexes on altered columns (like author_id) need explicit DROP INDEX IF EXISTS statements before the ALTER TABLE DROP COLUMN.
  </action>
  <verify>
- `ls graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/down.sql` -- file exists
- File starts with BEGIN, ends with COMMIT
- Count DROP TABLE IF EXISTS statements: should be 24
- Count ALTER TABLE DROP COLUMN statements: should be 11
- Verify junction tables are dropped before entity tables
- Verify columns are dropped before their referenced entity tables
  </verify>
  <done>
down.sql exists and properly reverses all changes from up.sql in correct dependency order: junction tables first, then column removals, then entity tables.
  </done>
</task>

</tasks>

<verification>
1. Both migration files exist in graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/
2. up.sql is valid SQL (BEGIN/COMMIT wrapped, no syntax errors detectable by inspection)
3. No overlap with existing migration -- up.sql contains only CREATE TABLE (new tables), ALTER TABLE ADD COLUMN, and CREATE INDEX
4. down.sql properly reverses up.sql in dependency-safe order
5. All 9 entity types from the UAT gap are covered: ModelCategory, Region, Person, Process, TimeInterval, CausalDiagram, Image, VariablePresentation, Intervention, plus Grid
6. All missing columns from the UAT gap are covered: software_version (5 cols), model_configuration (1 col), model_configuration_setup (5 cols)
7. All junction tables from the UAT gap are covered: hasProcess, hasGrid, hasEquation (covered by Grid), hasCausalDiagram, hasOutputTimeInterval, calibratedVariable, calibrationTargetVariable, relevantForIntervention, plus hasModelCategory, hasRegion, hasExplanationDiagram, hasInputVariable, hasOutputVariable, author
</verification>

<success_criteria>
- Migration directory exists with both up.sql and down.sql
- up.sql contains 10 new entity tables, 14 junction tables, 11 column additions, and 30+ indexes
- down.sql correctly reverses everything in dependency order
- Migration is purely additive (no ALTER COLUMN, no DROP, no modifications to existing schema)
- All entity types and properties identified in the UAT gap are represented
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-and-data-migration/01-03-SUMMARY.md`
</output>
