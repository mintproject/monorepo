---
phase: 01-schema-and-data-migration
plan: 04
type: execute
wave: 2
depends_on: [01-03]
files_modified:
  - graphql_engine/metadata/tables.yaml
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "All new modelcatalog_* tables are tracked in Hasura with correct relationships"
    - "Junction tables expose object and array relationships for nested GraphQL queries"
    - "New columns on existing tables are exposed in select permissions"
    - "Anonymous and user roles can read all new tables and columns"
  artifacts:
    - path: "graphql_engine/metadata/tables.yaml"
      provides: "Hasura metadata for all new and modified modelcatalog_* tables"
      contains: "modelcatalog_person"
  key_links:
    - from: "modelcatalog_person table entry"
      to: "modelcatalog_setup_author junction"
      via: "array_relationship on person_id FK"
      pattern: "name: setup_authors"
    - from: "modelcatalog_model_configuration entry"
      to: "modelcatalog_configuration_region junction"
      via: "array_relationship on configuration_id FK"
      pattern: "name: regions"
    - from: "modelcatalog_software_version entry"
      to: "new columns in select_permissions"
      via: "columns list includes short_description, limitations, etc."
      pattern: "short_description"
---

<objective>
Update Hasura metadata (tables.yaml) to track all new tables from the extended schema migration and update existing table entries with new columns and relationships.

Purpose: Without metadata tracking, the new tables and columns are invisible to the GraphQL API. This plan wires everything into Hasura so nested GraphQL queries work across the extended schema.

Output: Updated tables.yaml with entries for all 24 new tables (10 entity + 14 junction) and updated entries for 3 existing tables.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-and-data-migration/01-01-SUMMARY.md
@.planning/phases/01-schema-and-data-migration/01-03-PLAN.md
@graphql_engine/metadata/tables.yaml
@graphql_engine/migrations/1771105510000_modelcatalog_extended_schema/up.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update existing table entries in tables.yaml</name>
  <files>graphql_engine/metadata/tables.yaml</files>
  <action>
Read the current tables.yaml file. Modify the following existing modelcatalog_* table entries:

**1. modelcatalog_software_version** -- Add new relationships and columns:

Add array_relationships:
- `categories` -> modelcatalog_software_version_category via foreign_key_constraint_on column: software_version_id
- `processes` -> modelcatalog_software_version_process via foreign_key_constraint_on column: software_version_id
- `grids` -> modelcatalog_software_version_grid via foreign_key_constraint_on column: software_version_id
- `explanation_diagrams` -> modelcatalog_software_version_image via foreign_key_constraint_on column: software_version_id
- `input_variables` -> modelcatalog_software_version_input_variable via foreign_key_constraint_on column: software_version_id
- `output_variables` -> modelcatalog_software_version_output_variable via foreign_key_constraint_on column: software_version_id

Add new columns to BOTH anonymous and user select_permissions:
- short_description
- limitations
- parameterization
- runtime_estimation
- theoretical_basis

**2. modelcatalog_model_configuration** -- Add new relationships and columns:

Add array_relationships:
- `causal_diagrams` -> modelcatalog_configuration_causal_diagram via foreign_key_constraint_on column: configuration_id
- `time_intervals` -> modelcatalog_configuration_time_interval via foreign_key_constraint_on column: configuration_id
- `regions` -> modelcatalog_configuration_region via foreign_key_constraint_on column: configuration_id

Add new columns to BOTH anonymous and user select_permissions:
- has_model_result_table

**3. modelcatalog_model_configuration_setup** -- Add new relationships and columns:

Add object_relationships:
- `author` -> modelcatalog_person via foreign_key_constraint_on: author_id

Add array_relationships:
- `authors` -> modelcatalog_setup_author via foreign_key_constraint_on column: setup_id
- `calibrated_variables` -> modelcatalog_setup_calibrated_variable via foreign_key_constraint_on column: setup_id
- `calibration_targets` -> modelcatalog_setup_calibration_target via foreign_key_constraint_on column: setup_id

Add new columns to BOTH anonymous and user select_permissions:
- author_id
- calibration_interval
- calibration_method
- parameter_assignment_method
- valid_until

**4. modelcatalog_parameter** -- Add new relationship:

Add array_relationships:
- `interventions` -> modelcatalog_parameter_intervention via foreign_key_constraint_on column: parameter_id

(No new columns on parameter table, so no permission changes needed.)

IMPORTANT: Preserve ALL existing content in these table entries. Only ADD new relationships and columns -- do not remove or reorder existing ones. Keep the exact same YAML structure and indentation as the existing entries.
  </action>
  <verify>
Verify the modifications by checking:
- modelcatalog_software_version entry has 6 new array_relationships (total 7: configurations + 6 new)
- modelcatalog_software_version anonymous/user permissions include short_description, limitations, parameterization, runtime_estimation, theoretical_basis
- modelcatalog_model_configuration entry has 3 new array_relationships (total 6: setups + inputs + outputs + parameters + 3 new)
- modelcatalog_model_configuration anonymous/user permissions include has_model_result_table
- modelcatalog_model_configuration_setup has 1 new object_relationship (author) + 3 new array_relationships
- modelcatalog_model_configuration_setup permissions include author_id, calibration_interval, calibration_method, parameter_assignment_method, valid_until
- modelcatalog_parameter has 1 new array_relationship (interventions)
- tables.yaml is valid YAML (python3 -c "import yaml; yaml.safe_load(open('graphql_engine/metadata/tables.yaml'))")
  </verify>
  <done>
Existing table entries updated with new relationships pointing to junction tables and new columns exposed in select permissions for both roles.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add new entity and junction table entries to tables.yaml</name>
  <files>graphql_engine/metadata/tables.yaml</files>
  <action>
Append new table entries to tables.yaml for all 24 new tables. Follow the exact same YAML structure as existing modelcatalog_* entries. Each entry needs: table name/schema, relationships, and select_permissions for anonymous and user roles.

**Entity table entries (10 tables):**

1. **modelcatalog_person**
   - object_relationships: (none)
   - array_relationships:
     - `setup_authors` -> modelcatalog_setup_author via person_id
   - select_permissions columns: id, label, name

2. **modelcatalog_model_category**
   - object_relationships:
     - `parent_category` via foreign_key_constraint_on: parent_category_id
   - array_relationships:
     - `subcategories` -> modelcatalog_model_category via parent_category_id (self-ref)
     - `software_versions` -> modelcatalog_software_version_category via category_id
   - select_permissions columns: id, label, parent_category_id

3. **modelcatalog_region**
   - object_relationships:
     - `part_of` via foreign_key_constraint_on: part_of_id
   - array_relationships:
     - `subregions` -> modelcatalog_region via part_of_id (self-ref)
     - `configurations` -> modelcatalog_configuration_region via region_id
   - select_permissions columns: id, label, description, part_of_id

4. **modelcatalog_process**
   - array_relationships:
     - `software_versions` -> modelcatalog_software_version_process via process_id
   - select_permissions columns: id, label

5. **modelcatalog_time_interval**
   - array_relationships:
     - `configurations` -> modelcatalog_configuration_time_interval via time_interval_id
   - select_permissions columns: id, label, description, interval_value, interval_unit

6. **modelcatalog_causal_diagram**
   - array_relationships:
     - `configurations` -> modelcatalog_configuration_causal_diagram via causal_diagram_id
     - `diagram_parts` -> modelcatalog_diagram_part via causal_diagram_id
   - select_permissions columns: id, label

7. **modelcatalog_image**
   - array_relationships:
     - `software_versions` -> modelcatalog_software_version_image via image_id
   - select_permissions columns: id, label, description

8. **modelcatalog_variable_presentation**
   - array_relationships:
     - `input_variable_of` -> modelcatalog_software_version_input_variable via variable_id
     - `output_variable_of` -> modelcatalog_software_version_output_variable via variable_id
     - `calibrated_in` -> modelcatalog_setup_calibrated_variable via variable_id
     - `calibration_target_in` -> modelcatalog_setup_calibration_target via variable_id
   - select_permissions columns: id, label, description, has_long_name, has_short_name, has_standard_variable, uses_unit

9. **modelcatalog_intervention**
   - array_relationships:
     - `parameters` -> modelcatalog_parameter_intervention via intervention_id
   - select_permissions columns: id, label, description

10. **modelcatalog_grid**
    - array_relationships:
      - `software_versions` -> modelcatalog_software_version_grid via grid_id
    - select_permissions columns: id, label, description, has_dimension, has_shape, has_spatial_resolution, has_coordinate_system, grid_type

**Junction table entries (14 tables):**

For each junction table, create an entry with:
- Two object_relationships pointing to the parent tables (using foreign_key_constraint_on for each FK column)
- select_permissions for both anonymous and user roles listing all columns

Junction tables and their relationships:

11. `modelcatalog_software_version_category` -- object_relationships: `software_version` (software_version_id), `category` (category_id) -- columns: software_version_id, category_id
12. `modelcatalog_software_version_process` -- object_relationships: `software_version` (software_version_id), `process` (process_id) -- columns: software_version_id, process_id
13. `modelcatalog_software_version_grid` -- object_relationships: `software_version` (software_version_id), `grid` (grid_id) -- columns: software_version_id, grid_id
14. `modelcatalog_software_version_image` -- object_relationships: `software_version` (software_version_id), `image` (image_id) -- columns: software_version_id, image_id
15. `modelcatalog_software_version_input_variable` -- object_relationships: `software_version` (software_version_id), `variable` (variable_id) -- columns: software_version_id, variable_id
16. `modelcatalog_software_version_output_variable` -- object_relationships: `software_version` (software_version_id), `variable` (variable_id) -- columns: software_version_id, variable_id
17. `modelcatalog_configuration_causal_diagram` -- object_relationships: `configuration` (configuration_id), `causal_diagram` (causal_diagram_id) -- columns: configuration_id, causal_diagram_id
18. `modelcatalog_configuration_time_interval` -- object_relationships: `configuration` (configuration_id), `time_interval` (time_interval_id) -- columns: configuration_id, time_interval_id
19. `modelcatalog_configuration_region` -- object_relationships: `configuration` (configuration_id), `region` (region_id) -- columns: configuration_id, region_id
20. `modelcatalog_setup_author` -- object_relationships: `setup` (setup_id), `person` (person_id) -- columns: setup_id, person_id
21. `modelcatalog_setup_calibrated_variable` -- object_relationships: `setup` (setup_id), `variable` (variable_id) -- columns: setup_id, variable_id
22. `modelcatalog_setup_calibration_target` -- object_relationships: `setup` (setup_id), `variable` (variable_id) -- columns: setup_id, variable_id
23. `modelcatalog_parameter_intervention` -- object_relationships: `parameter` (parameter_id), `intervention` (intervention_id) -- columns: parameter_id, intervention_id
24. `modelcatalog_diagram_part` -- object_relationships: `causal_diagram` (causal_diagram_id) -- columns: causal_diagram_id, part_id, part_type (note: part_id has no FK constraint so no object_relationship for it)

IMPORTANT: All table entries must specify `schema: public`. All select_permissions must be for both `anonymous` and `user` roles with `filter: {}` (unrestricted read). Follow the exact indentation and structure of existing junction table entries (e.g., modelcatalog_configuration_input).
  </action>
  <verify>
Validate the complete tables.yaml:
1. `python3 -c "import yaml; data = yaml.safe_load(open('graphql_engine/metadata/tables.yaml')); mc = [t for t in data if t['table']['name'].startswith('modelcatalog_')]; print(f'modelcatalog tables: {len(mc)}')"` -- should show 36 (12 original + 24 new)
2. Verify every new table has anonymous and user select_permissions
3. Verify all junction tables have exactly 2 object_relationships (except modelcatalog_diagram_part which has 1)
4. Verify existing 12 original table entries are preserved (not duplicated, not modified beyond Task 1 changes)
5. `python3 -c "import yaml; yaml.safe_load(open('graphql_engine/metadata/tables.yaml'))"` -- valid YAML with no errors
  </verify>
  <done>
tables.yaml contains entries for all 36 modelcatalog_* tables (12 original + 24 new) with correct relationships and permissions. YAML is valid and parseable.
  </done>
</task>

</tasks>

<verification>
1. tables.yaml is valid YAML
2. Total modelcatalog_* table entries: 36 (12 original + 10 new entity + 14 new junction)
3. All new entity tables have select_permissions for anonymous and user
4. All junction tables have object_relationships to both parent tables
5. Existing tables have new array_relationships pointing through junction tables to new entity tables
6. New columns on existing tables appear in select_permissions for both roles
7. No existing table entries were removed or broken
</verification>

<success_criteria>
- tables.yaml is valid YAML with no parse errors
- 36 modelcatalog_* table entries present
- All new tables tracked with read permissions for anonymous and user
- Relationship graph enables nested GraphQL queries through all junction tables
- Existing relationship structure (from 01-01) is fully preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-schema-and-data-migration/01-04-SUMMARY.md`
</output>
