---
phase: 02-api-integration
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - model-catalog-api/src/mappers/resource-registry.ts
  - model-catalog-api/src/service.ts
  - model-catalog-api/src/mappers/__tests__/response.test.ts
  - model-catalog-api/src/__tests__/integration.test.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /v2.0.0/softwares returns relationship fields with v1.8.0 OWL names (hasVersion, not versions)"
    - "GET /v2.0.0/softwareversions returns hasConfiguration (not configurations)"
    - "GET /v2.0.0/modelconfigurations returns hasSetup, hasInput, hasOutput, hasParameter, hasRegion, hasCausalDiagram, hasOutputTimeInterval"
    - "GET /v2.0.0/modelconfigurationsetups returns hasInput, hasOutput, hasParameter, calibratedVariable, calibrationTargetVariable"
    - "GET /v2.0.0/softwares/{plain-uuid} returns the entity (not 404)"
    - "PUT and DELETE with plain UUID IDs also resolve correctly"
  artifacts:
    - path: "model-catalog-api/src/mappers/resource-registry.ts"
      provides: "v1.8.0 OWL property names as relationship keys"
    - path: "model-catalog-api/src/service.ts"
      provides: "ID prefix prepending for plain IDs in getById, update, deleteResource"
  key_links:
    - from: "model-catalog-api/src/mappers/resource-registry.ts"
      to: "model-catalog-api/src/mappers/response.ts"
      via: "relationship key names used as output JSON field names"
      pattern: "result\\[apiFieldName\\]"
    - from: "model-catalog-api/src/service.ts"
      to: "Hasura _by_pk queries"
      via: "ID_PREFIX prepended to plain IDs before GraphQL query"
      pattern: "ID_PREFIX"
---

<objective>
Fix relationship field names in API responses to match v1.8.0 OWL property names, and fix get-by-ID to accept plain UUIDs.

Purpose: UAT tests 3 and 4 failed because (1) relationship keys use short names (versions, inputs) instead of v1.8.0 names (hasVersion, hasInput), and (2) getById/update/delete pass raw path params to Hasura without prepending the URI prefix, causing 404s for plain UUID lookups.

Output: Corrected resource-registry.ts with v1.8.0 field names, corrected service.ts with ID prefix logic, updated tests.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-integration/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename relationship keys in resource-registry.ts to v1.8.0 OWL names</name>
  <files>model-catalog-api/src/mappers/resource-registry.ts</files>
  <action>
Rename the relationship keys (the Record keys in `relationships: { ... }`) in RESOURCE_REGISTRY to match v1.8.0 API field names. The Hasura relationship names (hasuraRelName) stay the same -- only the object keys change.

The v1.8.0 field names are derived from the FastAPI model aliases in model-catalog-fastapi/src/openapi_server/models/. Complete mapping:

**softwares:**
- `versions` -> `hasVersion` (alias="hasVersion" in software.py line 74)
- `author` stays `author` (alias="author")
- `authors` stays `author` -- WAIT: v1.8.0 uses `author` for both single and array. Keep existing `author` (object) and `authors` (junction array) as-is for now. The v1.8.0 API uses `author` as an array field. Since the current registry has both `author` (object FK) and `authors` (junction array), and the response mapper outputs both, keep `author` for the single FK relationship. For the junction array, also use `author` to match v1.8.0. BUT this creates a key collision. Resolution: remove the `authors` junction key entirely and rename it to `author` -- but that collides with the FK object relationship. The v1.8.0 API only has one `author` field that is an array. So merge: remove the object `author` relationship (the FK column `author_id` will still be output as a scalar `authorId`), and rename `authors` -> `author`.

Actually, looking at the v1.8.0 model more carefully: `author` is `Optional[List[EmulatorCopyrightHolderInner]]` -- it's always an array. The current registry has `author` as an object relationship (single FK) and `authors` as an array relationship (junction). Both produce output under different keys. To match v1.8.0 which has a single `author` array field, we should keep both relationships but make them work together. The simplest fix: keep `author` (object, FK) as-is since v1.8.0 wraps it in an array anyway. Rename `authors` junction to `author` would collide. So keep `authors` as `author` -- NO, that breaks.

SIMPLEST CORRECT FIX: Only rename the non-colliding relationship keys. Keep `author`/`authors` as-is for now (both work, they output under `author` and `authors` respectively -- `authors` isn't a v1.8.0 field but it doesn't hurt). Focus on the clearly wrong names:

**softwares** (and all software subtypes: models, empiricalmodels, hybridmodels, emulators, theory-guidedmodels, theory_guidedmodels, coupledmodels):
- `versions` -> `hasVersion`

**softwareversions:**
- `configurations` -> `hasConfiguration`
- `categories` -> `hasModelCategory`
- `processes` -> `hasProcess`
- `grids` -> `hasGrid`
- `explanationDiagrams` -> `hasExplanationDiagram`
- `inputVariables` -> `hasInputVariable`
- `outputVariables` -> `hasOutputVariable`

**modelconfigurations:**
- `setups` -> `hasSetup`
- `inputs` -> `hasInput`
- `outputs` -> `hasOutput`
- `parameters` -> `hasParameter`
- `causalDiagrams` -> `hasCausalDiagram`
- `timeIntervals` -> `hasOutputTimeInterval`
- `regions` -> `hasRegion`

**modelconfigurationsetups** (and configurationsetups alias):
- `inputs` -> `hasInput`
- `outputs` -> `hasOutput`
- `parameters` -> `hasParameter`
- `calibratedVariables` -> `calibratedVariable`
- `calibrationTargets` -> `calibrationTargetVariable`

**causaldiagrams:**
- `diagramParts` -> `hasPart` (v1.8.0 uses hasPart)

**parameters:**
- `interventions` -> `hasIntervention` (if v1.8.0 uses this name -- check; the parameter model likely uses `hasIntervention`)

**modelcategorys:**
- `parentCategory` stays `parentCategory` (this is fine)
- `subcategories` stays as-is

**regions:**
- `partOf` stays `partOf`
- `subregions` stays as-is

Do NOT change hasuraRelName values. Only change the object keys in the `relationships` record.
  </action>
  <verify>Run `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` to confirm no TypeScript errors. Manually inspect that keys like `hasVersion`, `hasConfiguration`, `hasInput`, `hasOutput`, `hasParameter`, `hasSetup` appear as relationship record keys.</verify>
  <done>All relationship keys in resource-registry.ts match v1.8.0 OWL property names. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Prepend ID_PREFIX for plain IDs in service.ts getById/update/delete</name>
  <files>model-catalog-api/src/service.ts</files>
  <action>
In `service.ts`, modify the `getById()`, `update()`, and `deleteResource()` methods to detect whether the decoded ID is already a full URI. If the decoded ID does NOT start with `https://`, prepend the resource's `idPrefix` (from `resourceConfig.idPrefix`) to form the full URI before querying Hasura.

Specifically, after the existing `const id = decodeURIComponent(req.params.id)` line in each method, add:

```typescript
const fullId = id.startsWith('https://') ? id : `${resourceConfig.idPrefix}${id}`
```

Then use `fullId` instead of `id` in the GraphQL variables object (`variables: { id: fullId }`).

Apply this change in exactly three places:
1. `getById()` (around line 129) -- change `variables: { id }` to `variables: { id: fullId }`
2. `update()` (around line 228) -- change `variables: { id, set: input }` to `variables: { id: fullId, set: input }`, and also update the re-fetch query to use `fullId`
3. `deleteResource()` (around line 294) -- change `variables: { id }` to `variables: { id: fullId }`
  </action>
  <verify>Run `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` to confirm TypeScript compiles. Then run `npx vitest run` to confirm existing tests still pass (the URI-encoded ID test already sends full URIs so should be unaffected).</verify>
  <done>getById, update, and deleteResource accept both plain IDs (e.g., "1bade4cb-d924-4253-bfa9-4c02b461396a") and full URI IDs, correctly prepending ID_PREFIX when needed.</done>
</task>

<task type="auto">
  <name>Task 3: Update unit and integration tests for renamed relationship keys</name>
  <files>model-catalog-api/src/mappers/__tests__/response.test.ts, model-catalog-api/src/__tests__/integration.test.ts</files>
  <action>
Update test assertions that reference old relationship key names to use the new v1.8.0 names.

**In response.test.ts:**
- "transforms array relationship to array of nested objects" test (line 153-173): Change `result['versions']` to `result['hasVersion']` since the softwares registry now uses `hasVersion` as the key.
- "omits relationship field when array is empty" test (line 185-193): Change `result['versions']` / `toHaveProperty('versions')` to use `'hasVersion'`.

**In integration.test.ts:**
- Test "list software" (line 119): Change `sw.versions` to `sw.hasVersion`.
- Test "getById setup with nested data" (line 217-222): Change `setup.inputs` to `setup.hasInput`, `setup.parameters` to `setup.hasParameter`.

Also add a new integration test that verifies plain ID lookup works:

```typescript
describe('getById with plain ID (no URI prefix)', () => {
  beforeEach(() => { mockQuery.mockReset() })

  it('prepends ID_PREFIX when id does not start with https://', async () => {
    mockQuery.mockResolvedValueOnce({
      data: {
        modelcatalog_software_by_pk: {
          id: 'https://w3id.org/okn/i/mint/1bade4cb-d924-4253-bfa9-4c02b461396a',
          label: 'TestSW',
          description: null, keywords: null, license: null, website: null,
          date_created: null, date_published: null, has_documentation: null,
          has_download_url: null, has_purpose: null, author_id: null, type: null,
          author: null, versions: [], authors: [],
        },
      },
    })

    const req = makeReq({ params: { id: '1bade4cb-d924-4253-bfa9-4c02b461396a' } })
    const reply = makeReply()
    await (CatalogService as any).softwares_id_get(req, reply)

    expect(reply._status).toBe(200)
    expect(mockQuery).toHaveBeenCalledOnce()
    const callArgs = mockQuery.mock.calls[0][0]
    expect(callArgs.variables.id).toBe('https://w3id.org/okn/i/mint/1bade4cb-d924-4253-bfa9-4c02b461396a')
  })
})
```
  </action>
  <verify>Run `cd /Users/mosorio/repos/model-catalog-api && npx vitest run`. All tests pass including the new plain-ID test.</verify>
  <done>All tests updated to reference v1.8.0 relationship names (hasVersion, hasInput, hasParameter, etc.) and a new test validates plain ID prefix prepending. Full test suite passes.</done>
</task>

</tasks>

<verification>
1. `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` -- TypeScript compiles
2. `cd /Users/mosorio/repos/model-catalog-api && npx vitest run` -- all tests pass
3. Spot check: `grep -c "hasVersion\|hasConfiguration\|hasInput\|hasOutput\|hasParameter\|hasSetup" model-catalog-api/src/mappers/resource-registry.ts` -- confirms v1.8.0 names present
4. Spot check: `grep "fullId\|startsWith.*https" model-catalog-api/src/service.ts` -- confirms ID prefix logic exists in getById/update/deleteResource
</verification>

<success_criteria>
- resource-registry.ts relationship keys match v1.8.0 OWL property names
- service.ts getById/update/deleteResource prepend ID_PREFIX for plain IDs
- All existing tests updated and passing
- New plain-ID integration test passing
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-11-SUMMARY.md`
</output>
