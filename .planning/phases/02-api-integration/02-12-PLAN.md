---
phase: 02-api-integration
plan: 12
type: execute
wave: 1
depends_on: []
files_modified:
  - model-catalog-api/src/custom-handlers.ts
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "GET /v2.0.0/custom/modelconfigurationsetups/{id} returns deeply nested setup with all relationships (no Hasura errors)"
    - "GET /v2.0.0/custom/modelconfigurations/{id} returns configuration with correct nested shapes"
    - "GET /v2.0.0/custom/model/index returns models with correct nested relationship shapes"
  artifacts:
    - path: "model-catalog-api/src/custom-handlers.ts"
      provides: "SETUP_FIELDS, SOFTWARE_FIELDS, CONFIGURATION_FIELDS with valid Hasura columns and junction traversal"
  key_links:
    - from: "model-catalog-api/src/custom-handlers.ts"
      to: "model-catalog-api/src/hasura/field-maps.ts"
      via: "SETUP_FIELDS must match actual columns defined in FIELD_SELECTIONS for modelcatalog_model_configuration_setup"
      pattern: "SETUP_FIELDS|SOFTWARE_FIELDS|CONFIGURATION_FIELDS"
---

<objective>
Rewrite the custom handler GraphQL field selection constants (SETUP_FIELDS, SOFTWARE_FIELDS, CONFIGURATION_FIELDS) to match the actual Hasura schema.

Purpose: UAT test 7 failed because SETUP_FIELDS requests non-existent columns (has_documentation, date_created, date_modified) and uses wrong nested shapes for junction table relationships. The field-maps.ts file contains the correct, authoritative field selections.

Output: Fixed custom-handlers.ts with valid Hasura field selections matching the real schema.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-integration/02-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite SETUP_FIELDS, SOFTWARE_FIELDS, and CONFIGURATION_FIELDS in custom-handlers.ts</name>
  <files>model-catalog-api/src/custom-handlers.ts</files>
  <action>
Replace the three field selection constants in custom-handlers.ts with correct Hasura-compatible field selections. Use field-maps.ts (model-catalog-api/src/hasura/field-maps.ts) as the authoritative reference for valid columns and junction traversal patterns.

**SETUP_FIELDS** (for modelcatalog_model_configuration_setup):

Current BROKEN fields to REMOVE: `has_documentation`, `date_created`, `date_modified` -- these columns do NOT exist on modelcatalog_model_configuration_setup.

Current BROKEN junction shapes to FIX: The current `inputs { id label ... }` treats junction rows as if they ARE the target entity. But junction tables (modelcatalog_setup_input, modelcatalog_setup_output, modelcatalog_setup_parameter) contain a nested relationship to the target entity. Must use junction traversal pattern: `inputs { input { id label ... } }`.

Replace SETUP_FIELDS with:
```
const SETUP_FIELDS = `
  id label description
  has_component_location has_implementation_script_location has_software_image has_region
  author_id calibration_interval calibration_method parameter_assignment_method valid_until
  model_configuration_id
  author { id label }
  model_configuration {
    id label description
    software_version { id label }
  }
  inputs {
    input {
      id label description has_format has_dimensionality position
    }
  }
  outputs {
    output {
      id label description has_format has_dimensionality position
    }
  }
  parameters {
    parameter {
      id label description has_data_type has_default_value
      has_minimum_accepted_value has_maximum_accepted_value has_fixed_value
      position parameter_type
    }
  }
  authors {
    person { id label }
  }
  calibrated_variables {
    variable { id label }
  }
  calibration_targets {
    variable { id label }
  }
`
```

**SOFTWARE_FIELDS** (for modelcatalog_software):

Current BROKEN fields: `has_documentation`, `date_created`, `date_modified` -- `date_modified` does NOT exist. `has_documentation` and `date_created` DO exist on modelcatalog_software, so keep those. Remove `date_modified`.

Current BROKEN junction shapes: `authors { id label }` should be `authors { person { id label } }`. The `versions` relationship is a direct array (not junction), so `versions { ... }` is correct. But inside versions, `configurations` is also direct, and inside configurations, `setups`, `inputs`, `outputs`, `parameters`, `regions` need junction traversal.

Replace SOFTWARE_FIELDS with:
```
const SOFTWARE_FIELDS = `
  id label description has_documentation date_created date_published
  keywords license website has_download_url has_purpose author_id type
  author { id label }
  authors {
    person { id label }
  }
  versions {
    id label description
    configurations {
      id label description
      setups { id label }
      inputs { input { id label } }
      outputs { output { id label } }
      parameters { parameter { id label } }
      regions { region { id label } }
    }
    input_variables { variable { id label } }
    output_variables { variable { id label } }
  }
`
```

**CONFIGURATION_FIELDS** (for modelcatalog_model_configuration):

Current BROKEN fields: `has_documentation`, `date_created`, `date_modified` -- `date_modified` does NOT exist. `has_documentation` does NOT exist on modelcatalog_model_configuration. Remove both `has_documentation` and `date_modified`. Keep `date_created` -- wait, check: the model_configuration table has columns: id, software_version_id, label, description, keywords, usage_notes, has_component_location, has_implementation_script_location, has_software_image, has_model_result_table, author_id. NO `has_documentation`, NO `date_created`, NO `date_modified`.

Current BROKEN junction shapes: Same pattern -- `inputs`, `outputs`, `parameters` need junction traversal, `causal_diagrams` and `time_intervals` and `regions` need junction traversal.

Replace CONFIGURATION_FIELDS with:
```
const CONFIGURATION_FIELDS = `
  id label description keywords usage_notes
  has_component_location has_implementation_script_location has_software_image has_model_result_table
  software_version_id author_id
  author { id label }
  authors {
    person { id label }
  }
  software_version { id label description }
  setups {
    id label description
    parameters { parameter { id label } }
    inputs { input { id label } }
    outputs { output { id label } }
  }
  inputs {
    input {
      id label description has_format has_dimensionality position
    }
  }
  outputs {
    output {
      id label description has_format has_dimensionality position
    }
  }
  parameters {
    parameter {
      id label description has_data_type has_default_value
      has_minimum_accepted_value has_maximum_accepted_value has_fixed_value
      position parameter_type
    }
  }
  causal_diagrams {
    causal_diagram { id label }
  }
  time_intervals {
    time_interval { id label description interval_value interval_unit }
  }
  regions {
    region { id label description }
  }
`
```

Also fix the inline GraphQL queries in individual handler functions that use these same wrong patterns:

- `custom_model_intervention_get` (line 138-149): The nested `versions { ... configurations { ... setups { ... parameters { ... interventions { ... } } } } }` query uses direct access for junction relationships. Fix: `parameters` inside setups needs junction traversal `parameters { parameter { id label interventions { intervention { id label } } } }`. Similarly `setups` inside configurations is direct (correct). But `inputs`/`outputs` in the variable-related handlers also need checking.

- `custom_model_region_get` (line 192): `regions { id label }` inside configurations needs junction traversal: `regions { region { id label } }`.

- `custom_models_variable_get` (line 235): `input_variables { id label }` and `output_variables { id label }` inside versions need junction traversal: `input_variables { variable { id label } }` and `output_variables { variable { id label } }`.

- `custom_modelconfigurationsetups_variable_get` (line 275): `inputs { id label variable_presented_as { id label } }` needs junction traversal: `inputs { input { id label } }` and `outputs { output { id label } }`. Note: `variable_presented_as` is not a real relationship on dataset_specification in our schema -- it's a scalar column. Remove it from the query.

- `custom_models_standard_variable_get` (line 400): Same fix as custom_models_variable_get.

- `custom_configuration_id_inputs_get` (line 517): `inputs { ... }` needs junction traversal: `inputs { input { id label description has_format has_dimensionality position } }`. Remove `variable_presented_as { id label }` (not a relationship).

- `custom_datasetspecifications_get` (line 451-457): The junction query `modelcatalog_configuration_input ... { dataset_specification { ... } }` and `modelcatalog_configuration_output ... { dataset_specification { ... } }` -- these query junction tables DIRECTLY which is correct (they use the junction table name as the root query, not through a relationship). The nested `dataset_specification` is the correct Hasura relationship name on the junction table. But check: is the junction relationship name `dataset_specification` or `input`/`output`? Looking at field-maps.ts, the junction relationship names are `input` and `output`. So in the direct junction query, it should be `input { ... }` not `dataset_specification { ... }`. Fix: change `dataset_specification` to `input` in the configuration_input query and `output` in the configuration_output query. Then update the JavaScript that extracts `.dataset_specification` to use `.input` and `.output` respectively.
  </action>
  <verify>
1. Run `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` -- TypeScript compiles
2. Run `cd /Users/mosorio/repos/model-catalog-api && npx vitest run` -- all tests pass
3. Spot check that SETUP_FIELDS no longer contains `has_documentation`, `date_created`, or `date_modified`
4. Spot check that junction traversal patterns `inputs { input { ... } }` are used throughout
  </verify>
  <done>All three field selection constants and all inline GraphQL queries in custom-handlers.ts use valid Hasura columns and correct junction traversal patterns matching field-maps.ts.</done>
</task>

</tasks>

<verification>
1. `cd /Users/mosorio/repos/model-catalog-api && npx tsc --noEmit` -- compiles
2. `cd /Users/mosorio/repos/model-catalog-api && npx vitest run` -- tests pass
3. Verify no references to `has_documentation` or `date_modified` in SETUP_FIELDS or CONFIGURATION_FIELDS
4. Verify all junction relationships use nested traversal pattern (e.g., `inputs { input { ... } }`)
</verification>

<success_criteria>
- SETUP_FIELDS only references columns that exist on modelcatalog_model_configuration_setup
- CONFIGURATION_FIELDS only references columns that exist on modelcatalog_model_configuration
- All junction relationships in custom handler queries use traversal pattern (inputs { input { } })
- TypeScript compiles and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-12-SUMMARY.md`
</output>
