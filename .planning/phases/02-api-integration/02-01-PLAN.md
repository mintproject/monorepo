---
phase: 02-api-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - graphql_engine/metadata/tables.yaml
autonomous: true

must_haves:
  truths:
    - "Hasura accepts insert, update, and delete mutations on all 36 modelcatalog_* tables for the user role"
    - "Anonymous role retains read-only access (no mutation permissions)"
  artifacts:
    - path: "graphql_engine/metadata/tables.yaml"
      provides: "insert_permissions, update_permissions, delete_permissions for all 36 modelcatalog tables"
      contains: "insert_permissions"
  key_links:
    - from: "graphql_engine/metadata/tables.yaml"
      to: "Hasura GraphQL engine"
      via: "Hasura metadata reload"
      pattern: "insert_permissions.*role: user"
---

<objective>
Add Hasura mutation permissions (insert, update, delete) for all 36 modelcatalog_* tables.

Purpose: Phase 1 only defined select_permissions. The new API needs to perform CRUD operations through Hasura GraphQL mutations, which requires insert/update/delete permissions on every modelcatalog table. This is a blocking prerequisite for write endpoints.
Output: Updated tables.yaml with full mutation permissions for the user role on all modelcatalog tables.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-schema-and-data-migration/01-01-SUMMARY.md
@.planning/phases/01-schema-and-data-migration/01-04-SUMMARY.md
@graphql_engine/metadata/tables.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add insert/update/delete permissions for 16 entity tables</name>
  <files>graphql_engine/metadata/tables.yaml</files>
  <action>
For each of the 16 modelcatalog entity tables (modelcatalog_software, modelcatalog_software_version, modelcatalog_model_configuration, modelcatalog_model_configuration_setup, modelcatalog_dataset_specification, modelcatalog_parameter, modelcatalog_person, modelcatalog_model_category, modelcatalog_region, modelcatalog_process, modelcatalog_time_interval, modelcatalog_causal_diagram, modelcatalog_image, modelcatalog_variable_presentation, modelcatalog_intervention, modelcatalog_grid), add three permission blocks to the existing table entry:

1. `insert_permissions` for role `user` with `check: {}` and all columns listed (copy the column list from the existing `select_permissions` for that table).
2. `update_permissions` for role `user` with `filter: {}`, `check: null`, and all columns listed (same column list).
3. `delete_permissions` for role `user` with `filter: {}`.

Follow the exact YAML structure used by existing non-modelcatalog tables in the file (e.g., `dataset`, `dataslice`) which already have these permission types. Do NOT add mutation permissions for the `anonymous` role -- anonymous stays read-only.

Place permissions in this order within each table entry: insert_permissions, select_permissions, update_permissions, delete_permissions (matching existing convention in the file).
  </action>
  <verify>
Run: `python3 -c "import yaml; data=yaml.safe_load(open('graphql_engine/metadata/tables.yaml')); mc=[t for t in data if 'modelcatalog' in t.get('table',{}).get('name','') and 'modelcatalog' in t['table']['name']]; entity=[t for t in mc if not any(j in t['table']['name'] for j in ['_input','_output','_parameter','_category_','_process_','_grid_','_image_','_variable_','_diagram_','_interval_','_region_','_author','_calibrat','_intervention','_part'])]; print(f'Entity tables with insert: {sum(1 for t in entity if \"insert_permissions\" in t)}/{len(entity)}'); print(f'Entity tables with update: {sum(1 for t in entity if \"update_permissions\" in t)}/{len(entity)}'); print(f'Entity tables with delete: {sum(1 for t in entity if \"delete_permissions\" in t)}/{len(entity)}')"` -- all counts should show 16/16.
  </verify>
  <done>All 16 modelcatalog entity tables have insert_permissions, update_permissions, and delete_permissions for the user role.</done>
</task>

<task type="auto">
  <name>Task 2: Add insert/update/delete permissions for 20 junction tables</name>
  <files>graphql_engine/metadata/tables.yaml</files>
  <action>
For each of the 20 modelcatalog junction tables (modelcatalog_configuration_input, modelcatalog_configuration_output, modelcatalog_configuration_parameter, modelcatalog_setup_input, modelcatalog_setup_output, modelcatalog_setup_parameter, modelcatalog_software_version_category, modelcatalog_software_version_process, modelcatalog_software_version_grid, modelcatalog_software_version_image, modelcatalog_software_version_input_variable, modelcatalog_software_version_output_variable, modelcatalog_configuration_causal_diagram, modelcatalog_configuration_time_interval, modelcatalog_configuration_region, modelcatalog_setup_author, modelcatalog_setup_calibrated_variable, modelcatalog_setup_calibration_target, modelcatalog_parameter_intervention, modelcatalog_diagram_part), add:

1. `insert_permissions` for role `user` with `check: {}` and all columns (the FK columns from select_permissions).
2. `delete_permissions` for role `user` with `filter: {}`.

Junction tables do NOT need update_permissions (rows are inserted/deleted, not updated -- they only contain FK pairs). Also add the software/version/configuration author junction tables: modelcatalog_software_author, modelcatalog_version_author, modelcatalog_configuration_author if they exist in the file.

Count the actual junction tables present in the file and ensure all get permissions.
  </action>
  <verify>
Run: `python3 -c "import yaml; data=yaml.safe_load(open('graphql_engine/metadata/tables.yaml')); mc=[t for t in data if 'modelcatalog' in t.get('table',{}).get('name','')]; jt=[t for t in mc if 'insert_permissions' not in str(t.get('insert_permissions',''))]; print(f'Total modelcatalog tables: {len(mc)}'); print(f'Tables with insert_permissions: {sum(1 for t in mc if \"insert_permissions\" in t)}'); print(f'Tables with delete_permissions: {sum(1 for t in mc if \"delete_permissions\" in t)}')"` -- all modelcatalog tables should have insert and delete permissions.
  </verify>
  <done>All modelcatalog junction tables have insert_permissions and delete_permissions for the user role. Every modelcatalog table in tables.yaml now supports mutations.</done>
</task>

</tasks>

<verification>
1. `python3 -c "import yaml; yaml.safe_load(open('graphql_engine/metadata/tables.yaml'))"` succeeds (valid YAML)
2. Every modelcatalog_* table has insert_permissions and delete_permissions for user role
3. Entity tables also have update_permissions for user role
4. Anonymous role has NO mutation permissions on any modelcatalog table
5. Existing non-modelcatalog table permissions are unchanged
</verification>

<success_criteria>
All 36 modelcatalog_* tables in Hasura metadata have mutation permissions (insert + delete for all; update for entity tables only), enabling the new API to perform full CRUD via GraphQL mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-01-SUMMARY.md`
</output>
