---
phase: 02-api-integration
plan: 07
type: execute
wave: 5
depends_on: ["02-01", "02-05", "02-06"]
files_modified:
  - helm-charts/charts/mint/templates/model-catalog-api-v2.yaml
  - helm-charts/charts/mint/templates/ingress-model-catalog-api-v2.yaml
  - helm-charts/charts/mint/values.yaml
  - /Users/mosorio/repos/model-catalog-api/Dockerfile
  - /Users/mosorio/repos/model-catalog-api/.github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "Docker image builds successfully from the model-catalog-api repo"
    - "Helm templates render valid Kubernetes manifests for the new API"
    - "values.yaml contains model_catalog_api_v2 component with image, ingress, and env config"
    - "CI pipeline runs lint, test, build, and pushes Docker image to Docker Hub"
    - "HASURA_GRAPHQL_URL is configurable via Helm values"
    - "Health endpoint is used as liveness probe"
  artifacts:
    - path: "helm-charts/charts/mint/templates/model-catalog-api-v2.yaml"
      provides: "Kubernetes Service + Deployment for new API"
      contains: "model-catalog-api-v2"
    - path: "helm-charts/charts/mint/templates/ingress-model-catalog-api-v2.yaml"
      provides: "Ingress routing for /v2.0.0/ paths"
      contains: "ingress"
    - path: "helm-charts/charts/mint/values.yaml"
      provides: "Configuration for model_catalog_api_v2 component"
      contains: "model_catalog_api_v2"
    - path: "/Users/mosorio/repos/model-catalog-api/Dockerfile"
      provides: "Multi-stage Docker build"
      contains: "FROM node"
    - path: "/Users/mosorio/repos/model-catalog-api/.github/workflows/ci.yml"
      provides: "CI/CD pipeline"
      contains: "docker"
  key_links:
    - from: "helm-charts/charts/mint/templates/model-catalog-api-v2.yaml"
      to: "helm-charts/charts/mint/values.yaml"
      via: "Helm template values"
      pattern: "model_catalog_api_v2"
    - from: "helm-charts/charts/mint/templates/model-catalog-api-v2.yaml"
      to: "/Users/mosorio/repos/model-catalog-api/Dockerfile"
      via: "Docker image reference"
      pattern: "mintproject/model-catalog-api"
---

<objective>
Create the Dockerfile, CI/CD pipeline, and Helm chart templates for deploying the new API.

Purpose: The new model-catalog-api needs to be containerized, built by CI, and deployable alongside the existing stack via Helm. This follows the existing helm-charts patterns in the mint repo.
Output: Dockerfile, GitHub Actions workflow, and Helm templates ready for deployment.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-api-integration/02-CONTEXT.md
@.planning/phases/02-api-integration/02-RESEARCH.md
@helm-charts/charts/mint/templates/model-catalog.yaml
@helm-charts/charts/mint/templates/ingress-model-catalog.yaml
@helm-charts/charts/mint/values.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Dockerfile and GitHub Actions CI workflow</name>
  <files>
    /Users/mosorio/repos/model-catalog-api/Dockerfile
    /Users/mosorio/repos/model-catalog-api/.github/workflows/ci.yml
  </files>
  <action>
**Dockerfile** (multi-stage build):
```dockerfile
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY tsconfig.json ./
COPY src/ ./src/
COPY openapi.yaml ./
RUN npm run build

FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --production
COPY --from=builder /app/dist ./dist
COPY openapi.yaml ./
ENV PORT=3000
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

Notes:
- Use node:20-alpine for small image size
- Production stage only includes production deps and compiled JS
- openapi.yaml is needed at runtime for fastify-openapi-glue
- Health check via /health endpoint (configured in Helm liveness probe, not Docker HEALTHCHECK)

**GitHub Actions CI** (`.github/workflows/ci.yml`):
```yaml
name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npx tsc --noEmit
      - run: npm test

  build-and-push:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          push: true
          tags: |
            mintproject/model-catalog-api:${{ github.sha }}
            mintproject/model-catalog-api:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
```

Docker image name per locked decision: `docker.io/mintproject/model-catalog-api`.
  </action>
  <verify>
Run from `/Users/mosorio/repos/model-catalog-api/`:
- `docker build -t model-catalog-api:test .` builds successfully (or `docker buildx build --load -t model-catalog-api:test .` if buildx is available)
- `cat .github/workflows/ci.yml` shows valid YAML with test and build-and-push jobs
  </verify>
  <done>Dockerfile builds a production image. CI pipeline runs lint, test, build, and pushes to Docker Hub on main branch pushes.</done>
</task>

<task type="auto">
  <name>Task 2: Create Helm templates and values for the new API</name>
  <files>
    helm-charts/charts/mint/templates/model-catalog-api-v2.yaml
    helm-charts/charts/mint/templates/ingress-model-catalog-api-v2.yaml
    helm-charts/charts/mint/values.yaml
  </files>
  <action>
Follow the exact patterns from the existing `model-catalog.yaml` and `ingress-model-catalog.yaml` templates.

**values.yaml** -- Add a new component block under `components:` (after the existing `model_catalog_api` block):

```yaml
  model_catalog_api_v2:
    # -- Enable or disable Model Catalog API v2
    enabled: false
    image:
      # -- Docker image repository for Model Catalog API v2
      repository: mintproject/model-catalog-api
      # -- Docker image tag for Model Catalog API v2
      tag: latest
      # -- Image pull policy for Model Catalog API v2
      pullPolicy: IfNotPresent
    # -- Resource specifications for Model Catalog API v2
    resources: {}
    environment:
      # -- Hasura GraphQL URL for Model Catalog API v2
      hasura_graphql_url: ""
      # -- Log level for Model Catalog API v2
      log_level: info
    ingress:
      # -- Enable or disable ingress for Model Catalog API v2
      enabled: true
      className: ""
      annotations:
        nginx.ingress.kubernetes.io/enable-cors: "true"
      hosts:
        - host: api.models.mint.local
          paths:
            - path: /v2.0.0
              pathType: Prefix
      tls: []
```

**model-catalog-api-v2.yaml** template:
```yaml
---
{{ if .Values.components.model_catalog_api_v2.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "mint.prefix" . }}-model-catalog-api-v2
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: "http-api"
      port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: {{ include "mint.prefix" . }}-model-catalog-api-v2
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "mint.prefix" . }}-model-catalog-api-v2
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "mint.prefix" . }}-model-catalog-api-v2
  template:
    metadata:
      labels:
        app: {{ include "mint.prefix" . }}-model-catalog-api-v2
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: model-catalog-api-v2
          {{- with .Values.components.model_catalog_api_v2 }}
          image: "{{ .image.repository }}:{{ .image.tag }}"
          imagePullPolicy: {{ .image.pullPolicy }}
          resources:
            {{- toYaml .resources | nindent 12 }}
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
          env:
            - name: PORT
              value: "3000"
            - name: HASURA_GRAPHQL_URL
              value: "{{ .environment.hasura_graphql_url }}"
            - name: LOG_LEVEL
              value: "{{ .environment.log_level }}"
          {{- end }}
            - name: HASURA_ADMIN_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "mint.prefix" . }}-secrets
                  key: hasura_admin_secret
                  optional: true
{{ end }}
```

**ingress-model-catalog-api-v2.yaml** template:
Follow the exact same pattern as `ingress-model-catalog.yaml` but using `.Values.components.model_catalog_api_v2` and the service name `model-catalog-api-v2`. Use the existing ingress template as a reference -- copy its structure replacing the component path references.

The ingress should route `/v2.0.0` path prefix to the new service, allowing the old and new APIs to coexist on the same domain (both v1.8.0 and v2.0.0 accessible at api.models.mint.tacc.utexas.edu).
  </action>
  <verify>
Run from `/Users/mosorio/repos/mint/`:
- `helm template test-release helm-charts/charts/mint/ --set components.model_catalog_api_v2.enabled=true --set components.model_catalog_api_v2.environment.hasura_graphql_url=http://hasura:8080/v1/graphql 2>&1 | grep model-catalog-api-v2 | head -5` shows rendered resources
- Or if helm is not installed: `python3 -c "import yaml; y=yaml.safe_load(open('helm-charts/charts/mint/values.yaml')); print('v2 config:', 'model_catalog_api_v2' in y.get('components', {}))"` confirms values exist
  </verify>
  <done>Helm templates and values ready for deploying model-catalog-api-v2 alongside existing services. Ingress routes /v2.0.0 paths to the new API. HASURA_GRAPHQL_URL configurable via values.</done>
</task>

</tasks>

<verification>
1. Docker image builds successfully
2. CI workflow is valid YAML with test and build-push jobs
3. Helm values include model_catalog_api_v2 component
4. Helm templates render valid Service, Deployment, and Ingress
5. /health endpoint used as liveness probe
6. HASURA_GRAPHQL_URL passed as environment variable
</verification>

<success_criteria>
The new API can be built into a Docker image, tested and pushed by CI, and deployed via Helm alongside the existing v1.8.0 API. Both API versions are accessible on the same domain through path-based routing.
</success_criteria>

<output>
After completion, create `.planning/phases/02-api-integration/02-07-SUMMARY.md`
</output>
