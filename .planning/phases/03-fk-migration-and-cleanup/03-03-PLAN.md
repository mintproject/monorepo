---
phase: 03-fk-migration-and-cleanup
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - mint-ensemble-manager/package.json
  - mint-ensemble-manager/src/classes/mint/model-catalog-functions.ts
  - mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts
  - mint-ensemble-manager/src/classes/mint/mint-types.ts
  - mint-ensemble-manager/src/classes/mint/__tests__/model-catalog-functions.test.ts
  - mint-ensemble-manager/src/classes/graphql/graphql_functions.ts
  - mint-ensemble-manager/src/api/api-v1/services/subTasksService.ts
  - mint-ensemble-manager/src/api/api-v1/services/threadsService.ts
  - mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelInputService.ts
  - mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelParameterService.ts
  - mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelParameterService.test.ts
  - mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelInputService.test.ts
  - mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql
  - mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-configuration.graphql
autonomous: true

must_haves:
  truths:
    - "@mintproject/modelcatalog_client SDK dependency is removed from package.json"
    - "No file in mint-ensemble-manager imports from @mintproject/modelcatalog_client"
    - "model-catalog-functions.ts is deleted (fetch-and-copy REST pattern removed)"
    - "model-catalog-graphql-adapter.ts is rewritten to use codegen types instead of SDK types"
    - "All service files that imported SDK types now use codegen types or inline interfaces"
    - "New GraphQL query files exist for looking up modelcatalog_model_configuration_setup and modelcatalog_model_configuration by PK"
    - "model_catalog_api is removed from MintPreferences interface"
  artifacts:
    - path: "mint-ensemble-manager/package.json"
      provides: "SDK dependency removed"
    - path: "mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql"
      provides: "GraphQL query for modelcatalog_model_configuration_setup_by_pk"
      contains: "modelcatalog_model_configuration_setup_by_pk"
    - path: "mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-configuration.graphql"
      provides: "GraphQL query for modelcatalog_model_configuration_by_pk"
      contains: "modelcatalog_model_configuration_by_pk"
    - path: "mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts"
      provides: "Rewritten adapter using codegen types for catalog-to-execution data conversion"
  key_links:
    - from: "mint-ensemble-manager/src/api/api-v1/services/subTasksService.ts"
      to: "mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql"
      via: "GraphQL query import for direct Hasura lookup"
      pattern: "get-modelcatalog-setup"
    - from: "mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts"
      to: "mint-ensemble-manager/src/classes/graphql/types.ts"
      via: "codegen types instead of SDK types"
      pattern: "Model_Insert_Input"
---

<objective>
Remove the @mintproject/modelcatalog_client SDK dependency from the Ensemble Manager, delete the fetch-and-copy REST pattern, rewrite the GraphQL adapter to use codegen types, and create new GraphQL queries for direct Hasura lookups of modelcatalog_* tables.

Purpose: The Ensemble Manager no longer needs the Model Catalog REST API -- it queries modelcatalog_* tables directly via Hasura GraphQL. This eliminates the SDK dependency (CLNP-02) and the fetch-and-copy pattern.
Output: Clean Ensemble Manager codebase with no SDK imports, new GraphQL queries for direct catalog lookups, and rewritten adapter.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fk-migration-and-cleanup/03-CONTEXT.md
@.planning/phases/03-fk-migration-and-cleanup/03-RESEARCH.md
@.planning/phases/03-fk-migration-and-cleanup/03-01-SUMMARY.md
@mint-ensemble-manager/src/classes/mint/model-catalog-functions.ts
@mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts
@mint-ensemble-manager/src/classes/mint/mint-types.ts
@mint-ensemble-manager/src/classes/graphql/graphql_functions.ts
@mint-ensemble-manager/src/api/api-v1/services/subTasksService.ts
@mint-ensemble-manager/src/api/api-v1/services/threadsService.ts
@mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelInputService.ts
@mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelParameterService.ts
@mint-ensemble-manager/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove SDK, delete model-catalog-functions.ts, create new GraphQL queries</name>
  <files>
    mint-ensemble-manager/package.json
    mint-ensemble-manager/src/classes/mint/model-catalog-functions.ts
    mint-ensemble-manager/src/classes/mint/__tests__/model-catalog-functions.test.ts
    mint-ensemble-manager/src/classes/mint/mint-types.ts
    mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql
    mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-configuration.graphql
  </files>
  <action>
    **Step 1: Remove SDK from package.json**
    Remove `"@mintproject/modelcatalog_client": "^8.0.0"` from the dependencies section of `mint-ensemble-manager/package.json`.

    **Step 2: Delete model-catalog-functions.ts entirely**
    Delete `mint-ensemble-manager/src/classes/mint/model-catalog-functions.ts` -- this is the fetch-and-copy REST pattern file. Per user decision: "model-catalog-functions.ts: remove entirely (fetch-and-copy pattern no longer needed)."

    Also delete its test file: `mint-ensemble-manager/src/classes/mint/__tests__/model-catalog-functions.test.ts`

    **Step 3: Remove model_catalog_api from MintPreferences**
    In `mint-ensemble-manager/src/classes/mint/mint-types.ts`:
    - Remove the line `model_catalog_api?: string;` from the MintPreferences interface

    **Step 4: Create new GraphQL query files for direct Hasura lookups**
    These replace the REST API calls removed in step 2. The Ensemble Manager's existing codegen setup will pick these up.

    Create `mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql`:
    ```graphql
    query get_modelcatalog_setup($id: String!) {
      modelcatalog_model_configuration_setup_by_pk(id: $id) {
        id
        label
        description
        has_software_image
        has_component_location
        setup_parameters {
          parameter {
            id
            label
            description
            has_default_value
            has_fixed_value
            has_minimum_accepted_value
            has_maximum_accepted_value
            parameter_type
            position
            has_accepted_values
            has_data_type
          }
        }
        setup_inputs {
          input {
            id
            label
            has_format
            position
            description
          }
        }
        setup_outputs {
          output {
            id
            label
            has_format
            description
          }
        }
      }
    }
    ```

    Create `mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-configuration.graphql`:
    ```graphql
    query get_modelcatalog_configuration($id: String!) {
      modelcatalog_model_configuration_by_pk(id: $id) {
        id
        label
        description
        has_component_location
        has_software_image
        configuration_parameters {
          parameter {
            id
            label
            description
            has_default_value
            has_fixed_value
            has_minimum_accepted_value
            has_maximum_accepted_value
            parameter_type
            position
            has_accepted_values
            has_data_type
          }
        }
        configuration_inputs {
          input {
            id
            label
            has_format
            position
            description
          }
        }
        configuration_outputs {
          output {
            id
            label
            has_format
            description
          }
        }
      }
    }
    ```

    Note: The relationship names (setup_parameters, setup_inputs, setup_outputs, configuration_parameters, etc.) must match the Hasura metadata relationship names in tables.yaml. Verify the exact names by checking tables.yaml for the modelcatalog_model_configuration_setup and modelcatalog_model_configuration entries. If they differ, use the correct names from tables.yaml.
  </action>
  <verify>
    - `grep "@mintproject/modelcatalog_client" mint-ensemble-manager/package.json` returns nothing
    - `test ! -f mint-ensemble-manager/src/classes/mint/model-catalog-functions.ts` succeeds (file deleted)
    - `test ! -f mint-ensemble-manager/src/classes/mint/__tests__/model-catalog-functions.test.ts` succeeds
    - `grep "model_catalog_api" mint-ensemble-manager/src/classes/mint/mint-types.ts` returns nothing
    - `test -f mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-setup.graphql` succeeds
    - `test -f mint-ensemble-manager/src/classes/graphql/queries/model/get-modelcatalog-configuration.graphql` succeeds
    - Both .graphql files contain `_by_pk(id: $id)` pattern
  </verify>
  <done>
    SDK removed from package.json. model-catalog-functions.ts and its test deleted. model_catalog_api removed from MintPreferences. Two new GraphQL query files created for direct Hasura lookups of model configuration and setup by PK.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite adapter and update all SDK import files</name>
  <files>
    mint-ensemble-manager/src/classes/mint/model-catalog-graphql-adapter.ts
    mint-ensemble-manager/src/classes/graphql/graphql_functions.ts
    mint-ensemble-manager/src/api/api-v1/services/subTasksService.ts
    mint-ensemble-manager/src/api/api-v1/services/threadsService.ts
    mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelInputService.ts
    mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelParameterService.ts
    mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelParameterService.test.ts
    mint-ensemble-manager/src/api/api-v1/services/useModelService/useModelInputService.test.ts
  </files>
  <action>
    Rewrite all files that import from `@mintproject/modelcatalog_client` to use either existing codegen types from `src/classes/graphql/types.ts` or inline TypeScript interfaces.

    **File 1: model-catalog-graphql-adapter.ts** -- REWRITE (per user decision)
    This file converts model catalog data to execution data (Model_Insert_Input). Rewrite to accept data shaped like the GraphQL query responses from Task 1's new .graphql files instead of SDK types.

    Replace SDK type imports (`DatasetSpecification`, `ModelConfiguration`, `ModelConfigurationSetup`, `Parameter`) with inline interfaces matching the shape of data returned by the new GraphQL queries. For example:
    ```typescript
    // Types matching GraphQL query response shapes
    interface CatalogDatasetSpec {
      id: string;
      label?: string;
      description?: string;
      has_format?: string;
      position?: number;
    }
    interface CatalogParameter {
      id: string;
      label?: string;
      description?: string;
      has_default_value?: string;
      has_fixed_value?: string;
      has_minimum_accepted_value?: string;
      has_maximum_accepted_value?: string;
      parameter_type?: string;
      position?: number;
      has_accepted_values?: string;
      has_data_type?: string;
    }
    ```

    Update all conversion functions (`modelInputToGraphQL`, `modelOutputToGraphQL`, `modelParameterToGraphQL`, `modelParametersToGraphQL`, `modelConfigurationToGraphQL`, `modelConfigurationSetupToGraphQL`) to:
    - Accept the new interfaces instead of SDK types
    - Map fields from the new GraphQL column names (e.g., `has_default_value` instead of `hasDefaultValue?.[0]`, `label` instead of `label?.[0]`)
    - The GraphQL response has scalar values (not arrays), so remove the `?.[0]` array unwrapping pattern throughout
    - Keep the return types (Model_Insert_Input, etc.) the same -- those are codegen types from the existing schema

    **File 2: graphql_functions.ts** (line ~21)
    Replace: `import { ModelConfigurationSetup } from "@mintproject/modelcatalog_client";`
    With: An inline interface or import from the rewritten adapter. The function that uses this type likely accepts a ModelConfigurationSetup and passes it to the adapter. Check what this function does and either:
    - Use the new CatalogSetup interface from the rewritten adapter
    - Or define a minimal inline type that satisfies the function's needs

    **File 3: subTasksService.ts** (lines ~23-24, ~42, ~277-280)
    Replace `fetchModelConfiguration` and `fetchModelConfigurationSetup` imports from model-catalog-functions.ts with direct Hasura GraphQL queries using the new .graphql files.
    - Import the GraphQL query documents: `import getModelcatalogSetupGQL from "@/classes/graphql/queries/model/get-modelcatalog-setup.graphql"` (and configuration equivalent)
    - Replace `fetchModelConfiguration(modelId)` with a GraphQL client query using `getModelcatalogConfigurationGQL`
    - Replace `fetchModelConfigurationSetup(modelId)` with a GraphQL query using `getModelcatalogSetupGQL`
    - Replace SDK type `DatasetSpecification` import with the inline CatalogDatasetSpec type or import from adapter
    - Use the existing GraphQL client from `GraphQL.instance()` or `GraphQL.instanceUsingAccessToken()`

    **File 4: threadsService.ts** (lines ~16, ~34, ~132)
    Replace `fetchModelFromCatalog` import (from deleted file) with direct GraphQL lookup:
    - Remove: `import { fetchModelFromCatalog } from "@/classes/mint/model-catalog-functions";`
    - Remove: `import { ModelConfigurationSetup } from "@mintproject/modelcatalog_client";`
    - The call at line ~132 fetches a model by response variables and model ID then copies it into the local DB. Replace this entire pattern:
      - Query `modelcatalog_model_configuration_setup` by matching the modelid (the old code does a variable-based lookup then matches by name -- simplify to a direct _by_pk query using the W3ID URI: `https://w3id.org/okn/i/mint/{modelid}`)
      - Pass the query result through the rewritten adapter to get Model_Insert_Input
    - If the `POST /threads` endpoint is legacy and rarely used, the replacement can be a simpler pattern

    **File 5: useModelInputService.ts** (lines ~8-12, ~120, ~135)
    Replace SDK imports with inline types or adapter imports:
    - Remove: `import { DatasetSpecification, ModelConfiguration, ModelConfigurationSetup } from "@mintproject/modelcatalog_client/dist";`
    - Remove: `import { fetchCustomModelConfigurationOrSetup } from "@/classes/mint/model-catalog-functions";`
    - Replace `fetchCustomModelConfigurationOrSetup(model_id)` calls with direct GraphQL queries
    - The function receives a model_id (which is a REST API URL like `https://api.models.mint.local/v1.8.0/modelconfigurations/UUID`) -- extract the UUID, construct the W3ID URI, then query via GraphQL

    **File 6: useModelParameterService.ts** (lines ~7-11, ~76, ~90)
    Same pattern as File 5:
    - Remove SDK imports
    - Remove model-catalog-functions imports
    - Replace with direct GraphQL queries

    **File 7: useModelParameterService.test.ts** (lines ~6-13, ~136, ~149, ~212)
    - Remove SDK imports (`ModelConfigurationSetup`, `Parameter`)
    - Remove mock for `fetchCustomModelConfigurationOrSetup`
    - Update test fixtures to use the new type shapes (scalar values instead of arrays)
    - Mock the GraphQL client instead of the REST fetch function

    **File 8: useModelInputService.test.ts** (similar pattern)
    - Same updates as File 7

    Important:
    - The GraphQL query response has flat scalar fields (label, description) not array-wrapped fields (label[0], description[0]) like the SDK. Every `?.[0]` pattern must be removed.
    - The model_id values in service files may be REST API URLs or W3ID URIs. When converting, extract the UUID suffix and prepend `https://w3id.org/okn/i/mint/` to match the modelcatalog_* table IDs.
    - Use `GraphQL.instance()` for queries that don't need user auth, or `GraphQL.instanceUsingAccessToken(token)` for authenticated queries.
  </action>
  <verify>
    - `grep -r "@mintproject/modelcatalog_client" mint-ensemble-manager/src/` returns nothing
    - `grep -r "model-catalog-functions" mint-ensemble-manager/src/` returns nothing (imports of the deleted file are gone)
    - `grep -r "model_catalog_api" mint-ensemble-manager/src/` returns nothing (MintPreferences cleaned in Task 1)
    - Check that model-catalog-graphql-adapter.ts compiles: the functions still export Model_Insert_Input-returning functions but accept new types
    - Check no `?.[0]` array unwrapping patterns remain in model-catalog-graphql-adapter.ts (GraphQL returns scalars)
    - Run: `cd mint-ensemble-manager && npx tsc --noEmit 2>&1 | head -20` to check for TypeScript compilation errors (some may exist due to codegen types not being regenerated, but no import errors)
  </verify>
  <done>
    All 8 files that imported @mintproject/modelcatalog_client are updated. model-catalog-graphql-adapter.ts rewritten with inline types matching GraphQL response shapes. All service files use direct GraphQL queries instead of REST API fetch-and-copy. No SDK imports remain anywhere in the codebase.
  </done>
</task>

</tasks>

<verification>
- Zero imports of @mintproject/modelcatalog_client in any file
- Zero imports of model-catalog-functions (deleted file)
- Zero references to model_catalog_api in TypeScript code
- model-catalog-graphql-adapter.ts uses flat scalar fields (not array-wrapped)
- New GraphQL query files exist and use correct Hasura relationship names
- Package.json has no SDK dependency
</verification>

<success_criteria>
- `grep -r "@mintproject/modelcatalog_client" mint-ensemble-manager/` returns only node_modules hits (if not cleaned) or nothing
- `grep -r "model-catalog-functions" mint-ensemble-manager/src/` returns nothing
- Two new .graphql query files exist for modelcatalog lookup
- TypeScript compilation has no import-related errors (type errors from missing codegen may exist until codegen runs against updated Hasura)
</success_criteria>

<output>
After completion, create `.planning/phases/03-fk-migration-and-cleanup/03-03-SUMMARY.md`
</output>
