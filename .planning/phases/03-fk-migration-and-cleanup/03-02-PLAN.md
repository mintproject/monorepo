---
phase: 03-fk-migration-and-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - helm-charts/charts/mint/values.yaml
  - helm-charts/charts/mint/templates/model-catalog-endpoint.yaml
  - helm-charts/charts/mint/templates/model-catalog-endpoint-backup.yaml
  - helm-charts/charts/mint/templates/ingress-model-catalog-endpoint.yaml
  - helm-charts/charts/mint/templates/post-install-model-catalog-endpoint.yaml
  - helm-charts/charts/mint/templates/ensemble-manager-config.yaml
  - mint-ensemble-manager/src/config/config.json
  - mint-ensemble-manager/src/config/config-tapis.json
autonomous: true

must_haves:
  truths:
    - "Fuseki (model_catalog_endpoint) is disabled in Helm chart values.yaml"
    - "All Fuseki Helm templates are guarded by enabled check so they do not deploy"
    - "model_catalog_api key is removed from the Ensemble Manager Helm ConfigMap template"
    - "model_catalog_api key is removed from local Ensemble Manager config files"
    - "PVC template for model_catalog is left intact with helm.sh/resource-policy: keep"
  artifacts:
    - path: "helm-charts/charts/mint/values.yaml"
      provides: "Fuseki disabled, model_catalog_api config removed"
      contains: "model_catalog_endpoint"
    - path: "helm-charts/charts/mint/templates/ensemble-manager-config.yaml"
      provides: "ConfigMap without model_catalog_api key"
  key_links:
    - from: "helm-charts/charts/mint/values.yaml"
      to: "helm-charts/charts/mint/templates/model-catalog-endpoint.yaml"
      via: "components.model_catalog_endpoint.enabled condition"
      pattern: "model_catalog_endpoint.*enabled.*false"
    - from: "helm-charts/charts/mint/templates/ensemble-manager-config.yaml"
      to: "mint-ensemble-manager/src/classes/mint/mint-types.ts"
      via: "ConfigMap generates config.json consumed by MintPreferences"
      pattern: "model_catalog_api"
---

<objective>
Disable Fuseki from the Helm chart deployment and remove the model_catalog_api configuration key from the Ensemble Manager ConfigMap and local config files.

Purpose: Fuseki is no longer needed since model catalog data lives in PostgreSQL/Hasura. The Ensemble Manager will query modelcatalog_* tables directly via Hasura GraphQL, so the REST API endpoint config is also unnecessary.
Output: Updated Helm values.yaml, guarded Helm templates, cleaned ConfigMap template, cleaned local config files.
</objective>

<execution_context>
@/Users/mosorio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mosorio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fk-migration-and-cleanup/03-CONTEXT.md
@.planning/phases/03-fk-migration-and-cleanup/03-RESEARCH.md
@helm-charts/charts/mint/templates/model-catalog-endpoint.yaml
@helm-charts/charts/mint/templates/ensemble-manager-config.yaml
@helm-charts/charts/mint/values.yaml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Disable Fuseki in Helm chart</name>
  <files>
    helm-charts/charts/mint/values.yaml
    helm-charts/charts/mint/templates/model-catalog-endpoint.yaml
    helm-charts/charts/mint/templates/model-catalog-endpoint-backup.yaml
    helm-charts/charts/mint/templates/ingress-model-catalog-endpoint.yaml
    helm-charts/charts/mint/templates/post-install-model-catalog-endpoint.yaml
  </files>
  <action>
    Disable Fuseki (model_catalog_endpoint) in the Helm chart. Per user decision: "Remove from Helm chart (deployment, service, configmap) and application code."

    **In `helm-charts/charts/mint/values.yaml`:**
    1. Find `components.model_catalog_endpoint.enabled: true` (line ~188) and change to `enabled: false`
    2. Find `init.model_catalog_endpoint.enabled: true` (line ~507) and change to `enabled: false`
    3. Leave the rest of the model_catalog_endpoint config (image, resources, persistence, secrets) as-is for reference -- just disabled

    **In Helm templates:**
    Verify each Fuseki-related template is already guarded by `{{ if .Values.components.model_catalog_endpoint.enabled }}`. Based on the research, `model-catalog-endpoint.yaml` already has this guard. Check each template and add the guard if missing:

    - `model-catalog-endpoint.yaml` -- already guarded (line 2: `{{ if .Values.components.model_catalog_endpoint.enabled }}`, ends with `{{ end }}`)
    - `model-catalog-endpoint-backup.yaml` -- check and add guard if missing. Pattern: wrap entire file content in `{{ if .Values.components.model_catalog_endpoint.enabled }}` ... `{{ end }}`
    - `ingress-model-catalog-endpoint.yaml` -- check and add guard if missing
    - `post-install-model-catalog-endpoint.yaml` -- check and add guard if missing

    Do NOT touch:
    - `pvc-model-catalog.yaml` -- PVC has `helm.sh/resource-policy: keep` annotation. Per Pitfall 7, leaving it intact is intentional for data preservation.
    - Docker Compose files or CI/CD configs -- deferred per user decision
    - `model-catalog.yaml` (the FastAPI v1.8.0 API template) -- stays running as fallback per user decision
  </action>
  <verify>
    Check values.yaml: `grep -n "enabled" helm-charts/charts/mint/values.yaml | grep model_catalog_endpoint` should show `false` for both entries.
    Check each template file starts with `{{ if .Values.components.model_catalog_endpoint.enabled }}` or equivalent guard.
    Run: `helm template test helm-charts/charts/mint/ --set components.model_catalog_endpoint.enabled=false 2>&1 | grep -c "model-catalog-endpoint"` should return 0 (no Fuseki resources rendered when disabled).
  </verify>
  <done>
    Fuseki components disabled in values.yaml. All 4 Fuseki Helm templates are guarded by enabled check. When enabled=false, no Fuseki Deployment, Service, backup job, ingress, or post-install hook is rendered. PVC template is left intact.
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove model_catalog_api from ConfigMap and config files</name>
  <files>
    helm-charts/charts/mint/templates/ensemble-manager-config.yaml
    mint-ensemble-manager/src/config/config.json
    mint-ensemble-manager/src/config/config-tapis.json
  </files>
  <action>
    Remove the `model_catalog_api` configuration key from all config sources. Per user decision: "Ensemble Manager: remove model_catalog_api config entirely -- no longer needs the REST API since it queries modelcatalog_* tables directly via Hasura GraphQL."

    **In `helm-charts/charts/mint/templates/ensemble-manager-config.yaml`:**
    1. Remove the line: `"model_catalog_api": "http://{{ include "mint.prefix" . }}-model-catalog/{{ .Values.components.model_catalog_api.api_version }}",`
    2. Ensure the resulting JSON is still valid (check commas, no trailing comma before next key)

    **In `mint-ensemble-manager/src/config/config.json`:**
    1. Remove the `"model_catalog_api": "http://api.models.mint.local/v1.8.0"` line (line ~17)
    2. Fix JSON formatting (commas)

    **In `mint-ensemble-manager/src/config/config-tapis.json`:**
    1. Remove the `"model_catalog_api": "http://api.models.mint.local/v1.8.0"` line (line ~3)
    2. Fix JSON formatting (commas)

    Important: Do NOT yet remove `model_catalog_api` from the `MintPreferences` TypeScript interface or from any TypeScript import files -- that is handled in Plan 03 (SDK removal) to keep the code change atomic. The config files can be cleaned first because the TypeScript code that reads this key will be removed in Plan 03 as well.
  </action>
  <verify>
    Verify no `model_catalog_api` key appears in any of the three files:
    - `grep "model_catalog_api" helm-charts/charts/mint/templates/ensemble-manager-config.yaml` should return nothing
    - `grep "model_catalog_api" mint-ensemble-manager/src/config/config.json` should return nothing
    - `grep "model_catalog_api" mint-ensemble-manager/src/config/config-tapis.json` should return nothing
    Verify JSON validity: `python3 -c "import json; json.load(open('mint-ensemble-manager/src/config/config.json'))"` should succeed.
  </verify>
  <done>
    model_catalog_api key removed from Helm ConfigMap template and both local config files. JSON remains valid. TypeScript code cleanup deferred to Plan 03 for atomicity.
  </done>
</task>

</tasks>

<verification>
- Fuseki disabled in values.yaml (both component and init sections)
- All Fuseki Helm templates guarded by enabled check
- model_catalog_api removed from 3 config files
- JSON files remain valid
- PVC template untouched
- model-catalog.yaml (v1.8.0 FastAPI) untouched
</verification>

<success_criteria>
- `helm template` with model_catalog_endpoint.enabled=false renders zero Fuseki resources
- No `model_catalog_api` key in any config file
- Existing v1.8.0 API deployment and PVC are unaffected
</success_criteria>

<output>
After completion, create `.planning/phases/03-fk-migration-and-cleanup/03-02-SUMMARY.md`
</output>
