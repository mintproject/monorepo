name: Deploy MINT Dev Pods

on:
  workflow_run:
    workflows: [MINT Dev Images]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  packages: read

concurrency:
  group: mint-dev-pods
  cancel-in-progress: false

jobs:
  restart:
    name: Restart MINT dev application pods
    runs-on: ubuntu-latest
    environment: Tapis Dev Deploy
    if: >-
      ${{
        github.event_name == 'workflow_dispatch' ||
        (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.head_branch == 'develop')
      }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deploy dependencies
        run: pip install tapipy python-dotenv

      # A later step can fail and skip the pods it owns, so grant ADMIN on
      # every pod first. This step only sets permissions.
      - name: Grant pod owners on every existing pod
        env:
          TAPIS_USERNAME: ${{ secrets.TAPIS_USERNAME }}
          TAPIS_ID: ${{ secrets.TAPIS_ID }}
          TAPIS_PASSWORD: ${{ secrets.TAPIS_PASSWORD }}
        run: >-
          python deploy/tapis/register_mint_stack.py
          --set-owners-only
          --pods all

      # The UI allowlist lives in the pod definition, and the deploy reaches
      # the UI pod last. Push the allowlist now, and keep the running image.
      - name: Sync the UI auth allowlist
        env:
          TAPIS_USERNAME: ${{ secrets.TAPIS_USERNAME }}
          TAPIS_ID: ${{ secrets.TAPIS_ID }}
          TAPIS_PASSWORD: ${{ secrets.TAPIS_PASSWORD }}
        run: >-
          python deploy/tapis/register_mint_stack.py
          --sync-ui-auth

      - name: Update PostgreSQL and Hasura pod definitions
        env:
          TAPIS_USERNAME: ${{ secrets.TAPIS_USERNAME }}
          TAPIS_ID: ${{ secrets.TAPIS_ID }}
          TAPIS_PASSWORD: ${{ secrets.TAPIS_PASSWORD }}
          MINTDEV_POSTGRES_PASSWORD: ${{ secrets.MINTDEV_POSTGRES_PASSWORD }}
          HASURA_GRAPHQL_ADMIN_SECRET: ${{ secrets.HASURA_GRAPHQL_ADMIN_SECRET }}
        run: >-
          python deploy/tapis/register_mint_stack.py
          --image-tag develop
          --pods postgres,graphql
          --restart-pods postgres,graphql
          --migrate-postgres-image

      - name: Log in to GHCR for Hasura migration client
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Apply Hasura migrations
        env:
          HASURA_GRAPHQL_ADMIN_SECRET: ${{ secrets.HASURA_GRAPHQL_ADMIN_SECRET }}
          GRAPHQL_ENDPOINT: https://mintdevgraphql.pods.portals.tapis.io
          IMAGE_TAG: develop
        run: |
          set -euo pipefail

          # Tapis starts the pod asynchronously. Wait for the endpoint before
          # asking the CLI to connect, with a bounded retry window.
          curl --fail --silent --show-error --retry 30 --retry-delay 5 \
            --retry-all-errors "$GRAPHQL_ENDPOINT/healthz" >/dev/null

          # The image contains the CLI and the /hasura project. Running the
          # same resolved tag keeps migrations and metadata aligned with the
          # GraphQL image that was just deployed.
          docker run --rm --pull=always \
            --entrypoint hasura \
            --workdir /hasura \
            --env HASURA_GRAPHQL_ENDPOINT="$GRAPHQL_ENDPOINT" \
            --env HASURA_GRAPHQL_ADMIN_SECRET \
            "ghcr.io/mintproject/graphql-engine:$IMAGE_TAG" \
            migrate status --skip-update-check

          docker run --rm --pull=always \
            --entrypoint hasura \
            --workdir /hasura \
            --env HASURA_GRAPHQL_ENDPOINT="$GRAPHQL_ENDPOINT" \
            --env HASURA_GRAPHQL_ADMIN_SECRET \
            "ghcr.io/mintproject/graphql-engine:$IMAGE_TAG" \
            migrate apply --skip-update-check

      - name: Start semantic search then restart Hasura endpoint
        env:
          TAPIS_USERNAME: ${{ secrets.TAPIS_USERNAME }}
          TAPIS_ID: ${{ secrets.TAPIS_ID }}
          TAPIS_PASSWORD: ${{ secrets.TAPIS_PASSWORD }}
          MINTDEV_POSTGRES_PASSWORD: ${{ secrets.MINTDEV_POSTGRES_PASSWORD }}
          HASURA_GRAPHQL_ADMIN_SECRET: ${{ secrets.HASURA_GRAPHQL_ADMIN_SECRET }}
        run: |
          set -euo pipefail

          # parse_pods preserves dependency order, so use separate invocations
          # to guarantee the migrated database is available before semantic
          # search starts, and semantic search is available before Hasura's
          # metadata is applied.
          python deploy/tapis/register_mint_stack.py \
            --image-tag develop \
            --pods semantic_search \
            --restart-pods semantic_search \
            --recreate-on-image-mismatch

          python deploy/tapis/register_mint_stack.py \
            --image-tag develop \
            --pods graphql \
            --restart-pods graphql \
            --recreate-on-image-mismatch

      - name: Verify semantic search and Hasura health
        env:
          SEMANTIC_SEARCH_ENDPOINT: https://mintdevsemanticsearch.pods.portals.tapis.io
          GRAPHQL_ENDPOINT: https://mintdevgraphql.pods.portals.tapis.io
        run: |
          set -euo pipefail
          curl --fail --silent --show-error --retry 30 --retry-delay 5 \
            --retry-all-errors --connect-timeout 10 --max-time 30 \
            "$SEMANTIC_SEARCH_ENDPOINT/health" >/dev/null
          curl --fail --silent --show-error --retry 30 --retry-delay 5 \
            --retry-all-errors --connect-timeout 10 --max-time 30 \
            "$GRAPHQL_ENDPOINT/healthz" >/dev/null

      - name: Apply Hasura metadata and verify schema
        env:
          HASURA_GRAPHQL_ADMIN_SECRET: ${{ secrets.HASURA_GRAPHQL_ADMIN_SECRET }}
          GRAPHQL_ENDPOINT: https://mintdevgraphql.pods.portals.tapis.io
          IMAGE_TAG: develop
        run: |
          set -euo pipefail

          docker run --rm --pull=always \
            --entrypoint hasura \
            --workdir /hasura \
            --env HASURA_GRAPHQL_ENDPOINT="$GRAPHQL_ENDPOINT" \
            --env HASURA_GRAPHQL_ADMIN_SECRET \
            "ghcr.io/mintproject/graphql-engine:$IMAGE_TAG" \
            metadata apply --skip-update-check

          docker run --rm --pull=always \
            --entrypoint hasura \
            --workdir /hasura \
            --env HASURA_GRAPHQL_ENDPOINT="$GRAPHQL_ENDPOINT" \
            --env HASURA_GRAPHQL_ADMIN_SECRET \
            "ghcr.io/mintproject/graphql-engine:$IMAGE_TAG" \
            metadata reload --skip-update-check

          # A bounded, read-only schema smoke test catches both classes of
          # regression without printing rows or GraphQL error payloads.
          response="$(curl --fail --silent --show-error \
            -H "content-type: application/json" \
            -H "x-hasura-admin-secret: $HASURA_GRAPHQL_ADMIN_SECRET" \
            --data '{"query":"{ modelcatalog_etl_process(limit: 1) { id } problem_statement(limit: 1) { id events(limit: 1) { event } } }"}' \
            "$GRAPHQL_ENDPOINT/v1/graphql")"
          if [[ "$response" == *'"errors"'* ]]; then
            echo "GraphQL schema smoke test failed" >&2
            exit 1
          fi
          echo "Hasura migrations, metadata, and schema smoke test passed"

      - name: Restart existing application pods
        env:
          TAPIS_USERNAME: ${{ secrets.TAPIS_USERNAME }}
          TAPIS_ID: ${{ secrets.TAPIS_ID }}
          TAPIS_PASSWORD: ${{ secrets.TAPIS_PASSWORD }}
        run: >-
          python deploy/tapis/register_mint_stack.py
          --restart-existing-pods "api,ui,ensemble,svo"

      - name: Summary
        run: |
          echo "## MINT dev application pods restarted" >> "$GITHUB_STEP_SUMMARY"
          echo "- UI: https://mintdevui.pods.portals.tapis.io" >> "$GITHUB_STEP_SUMMARY"
          echo "- API: https://mintdevapi.pods.portals.tapis.io" >> "$GITHUB_STEP_SUMMARY"
          echo "- Ensemble: https://mintdevensemble.pods.portals.tapis.io" >> "$GITHUB_STEP_SUMMARY"
          echo "- SVO Adapter: https://mintdevsvo.pods.portals.tapis.io" >> "$GITHUB_STEP_SUMMARY"
          echo "- Semantic Search: https://mintdevsemanticsearch.pods.portals.tapis.io" >> "$GITHUB_STEP_SUMMARY"
